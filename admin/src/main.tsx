import React from "react";
import ReactDOM from "react-dom/client";
import "./app.css";

// Placeholder - will be replaced with router setup in Task 2
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>Lumio Admin</div>
  </React.StrictMode>,
);
