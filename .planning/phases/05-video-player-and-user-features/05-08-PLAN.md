---
phase: 05-video-player-and-user-features
plan: 08
type: execute
wave: 3
depends_on: ["05-04", "05-05", "05-07"]
files_modified:
  - client/src/components/content/ContentCard.tsx
  - client/src/components/content/HoverPopover.tsx
  - client/src/components/detail/DetailModal.tsx
  - client/src/components/player/PlayerControls.tsx
  - client/src/components/layout/Navbar.tsx
  - client/src/app/page.tsx
  - client/src/api/client.ts
autonomous: true

must_haves:
  truths:
    - "MyListButton appears on ContentCard hover popover"
    - "MyListButton appears in DetailModal"
    - "MyListButton appears in player controls"
    - "Continue Watching row appears on home page"
    - "Play buttons throughout the app navigate to /watch/[id]"
    - "Navbar has My List link"
    - "API client sends auth token for authenticated requests"
  artifacts:
    - path: "client/src/components/content/ContentCard.tsx"
      provides: "Updated card with progress bar overlay"
    - path: "client/src/components/layout/Navbar.tsx"
      provides: "Updated navbar with My List link"
    - path: "client/src/app/page.tsx"
      provides: "Home page with Continue Watching row"
  key_links:
    - from: "client/src/components/content/HoverPopover.tsx"
      to: "client/src/components/my-list/MyListButton.tsx"
      via: "renders MyListButton in action buttons"
      pattern: "MyListButton"
    - from: "client/src/app/page.tsx"
      to: "client/src/components/content/ContinueWatchingRow.tsx"
      via: "renders Continue Watching row above other rows"
      pattern: "ContinueWatchingRow"
    - from: "client/src/api/client.ts"
      to: "Authorization header"
      via: "axios interceptor adding Bearer token"
      pattern: "Authorization|Bearer"
---

<objective>
Wire My List buttons, Continue Watching row, Play navigation, and auth token into existing components across the app.

Purpose: Connect all Phase 5 features into the existing browsing experience built in Phase 4.
Output: My List toggle visible everywhere, Continue Watching on home page, Play buttons work, API auth ready.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-video-player-and-user-features/05-CONTEXT.md
@.planning/phases/05-video-player-and-user-features/05-05-SUMMARY.md
@.planning/phases/05-video-player-and-user-features/05-07-SUMMARY.md
@client/src/components/content/ContentCard.tsx
@client/src/components/content/HoverPopover.tsx
@client/src/components/detail/DetailModal.tsx
@client/src/components/layout/Navbar.tsx
@client/src/app/page.tsx
@client/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MyListButton and Play navigation to existing components</name>
  <files>
    client/src/components/content/HoverPopover.tsx
    client/src/components/detail/DetailModal.tsx
    client/src/components/player/PlayerControls.tsx
    client/src/components/layout/Navbar.tsx
  </files>
  <action>
1. Update `client/src/components/content/HoverPopover.tsx`:
   - Import MyListButton from "@/components/my-list/MyListButton"
   - Add MyListButton to the action buttons row (alongside existing Play, Like, Expand buttons)
   - Position: after Play button, before other buttons
   - Size: "sm"

2. Update `client/src/components/detail/DetailModal.tsx`:
   - Import MyListButton from "@/components/my-list/MyListButton"
   - Add MyListButton next to the Play button in the action buttons area
   - Size: "md"
   - Update Play button: make it navigate to /watch/[contentId] (use Next.js router.push)
   - For series: Play button should navigate to /watch/[contentId]?episode=[firstEpisodeId]

3. Update `client/src/components/player/PlayerControls.tsx`:
   - Import MyListButton from "@/components/my-list/MyListButton"
   - Add MyListButton to the top-right area of player controls (near the close button)
   - Size: "sm"
   - The contentId comes from VideoPlayer props

4. Update `client/src/components/layout/Navbar.tsx`:
   - Add "My List" link to the navbar navigation items
   - href="/my-list"
   - Add "Account" link as well (for /account page)
   - Position: in the nav links area, after existing links
  </action>
  <verify>cd client && npx tsc --noEmit</verify>
  <done>MyListButton appears in popover, detail modal, and player. Navbar has My List and Account links.</done>
</task>

<task type="auto">
  <name>Task 2: Continue Watching on home page and API auth setup</name>
  <files>
    client/src/app/page.tsx
    client/src/api/client.ts
    client/src/components/content/ContentCard.tsx
  </files>
  <action>
1. Update `client/src/app/page.tsx` (home page):
   - Import ContinueWatchingRow from "@/components/content/ContinueWatchingRow"
   - Render ContinueWatchingRow ABOVE the existing content rows (after hero banner, before browse rows)
   - The ContinueWatchingRow handles its own data fetching and returns null if empty

2. Update `client/src/api/client.ts`:
   - Add an axios request interceptor that attaches Authorization header:
     - Read auth token from localStorage (key: "token" -- match whatever key the auth flow will use)
     - If token exists, set `config.headers.Authorization = "Bearer " + token`
     - NOTE: Full auth flow (login page, token storage) will come in a later phase. For now, set up the interceptor so authenticated endpoints work when a token is present.
   - This enables all Phase 5 API calls (progress, my-list, user) to authenticate

3. Update `client/src/components/content/ContentCard.tsx`:
   - Add optional progressPercent prop (number | undefined)
   - When progressPercent is provided, render a thin red progress bar at the bottom of the card image area
   - The bar is absolute positioned at bottom, height 3px, bg-red-600, width = progressPercent%
   - This is used by ContinueWatchingRow to show resume progress on cards
  </action>
  <verify>cd client && npx tsc --noEmit</verify>
  <done>Home page shows Continue Watching row, API client sends auth token, ContentCard can show progress bar</done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` passes
- MyListButton rendered in HoverPopover, DetailModal, and PlayerControls
- Play buttons in detail modal navigate to /watch/[id]
- Navbar has "My List" and "Account" links
- Home page includes ContinueWatchingRow above browse rows
- API client has auth interceptor for Bearer token
- ContentCard accepts optional progressPercent prop
</verification>

<success_criteria>
- All My List surfaces connected (3 locations + dedicated page)
- Play buttons navigate to watch page
- Continue Watching visible on home page (when data exists)
- Content cards can display progress bar overlay
- Auth token sent with all API requests when available
- Navbar updated with new navigation items
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-player-and-user-features/05-08-SUMMARY.md`
</output>
