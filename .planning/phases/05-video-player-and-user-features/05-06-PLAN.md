---
phase: 05-video-player-and-user-features
plan: 06
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - client/src/components/account/ProfileSection.tsx
  - client/src/components/account/SubscriptionSection.tsx
  - client/src/components/account/DeviceSection.tsx
  - client/src/components/account/PreferencesSection.tsx
  - client/src/app/account/page.tsx
autonomous: true

must_haves:
  truths:
    - "Account page shows profile card with initials avatar, email (read-only), editable name and phone"
    - "Subscription section shows plan name, status, days remaining, and Manage link"
    - "Device section shows all sessions with remove button (except current)"
    - "Preferences section has newsletter and auto-renew toggles"
  artifacts:
    - path: "client/src/app/account/page.tsx"
      provides: "Account settings page"
      min_lines: 30
    - path: "client/src/components/account/ProfileSection.tsx"
      provides: "Profile editing section"
    - path: "client/src/components/account/DeviceSection.tsx"
      provides: "Device list with session management"
  key_links:
    - from: "client/src/app/account/page.tsx"
      to: "client/src/api/user.ts"
      via: "TanStack Query for profile, subscription, sessions"
      pattern: "useQuery|useMutation"
    - from: "client/src/components/account/DeviceSection.tsx"
      to: "client/src/api/user.ts"
      via: "getUserSessions + deleteSession"
      pattern: "getUserSessions|deleteSession"
---

<objective>
Build the Account Settings page with Profile, Subscription, Devices, and Preferences sections.

Purpose: Users can manage their profile, view subscription status, manage devices, and set preferences.
Output: Single scrollable /account page with 4 distinct sections.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-video-player-and-user-features/05-CONTEXT.md
@.planning/phases/05-video-player-and-user-features/05-03-SUMMARY.md
@client/src/api/user.ts
@client/src/types/player.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile and Subscription sections</name>
  <files>
    client/src/components/account/ProfileSection.tsx
    client/src/components/account/SubscriptionSection.tsx
  </files>
  <action>
1. Create `client/src/components/account/ProfileSection.tsx`:
   - "use client" directive
   - Uses TanStack Query: useQuery for getUserProfile, useMutation for updateUserProfile
   - Avatar: circular div with initials (first letter of name, uppercase), bg-red-600 text-white, large size (w-20 h-20)
   - Email: displayed as read-only text (gray, with "Email cannot be changed" note)
   - Name: inline editable field. Show current name with an "Edit" button. On click, switch to input + Save/Cancel buttons. On save, call updateUserProfile({ name }). Optimistic update via query invalidation.
   - Phone: same inline edit pattern as name. Call updateUserProfile({ phone }).
   - Section wrapper: card-like container with "Profile" heading, padding, rounded border

2. Create `client/src/components/account/SubscriptionSection.tsx`:
   - "use client" directive
   - Uses useQuery for getUserSubscription
   - Shows: plan name (or "No active plan"), status badge (green for active, red for expired), days remaining (calculated from endDate - now)
   - "Manage Subscription" link -> /billing (Link component, even though /billing doesn't exist yet -- it will in Phase 7)
   - If no subscription: show "No active subscription" with a "Subscribe" link to /billing
   - Section wrapper: same card style as Profile
  </action>
  <verify>cd client && npx tsc --noEmit</verify>
  <done>Profile section renders editable name/phone with save, Subscription section shows plan info</done>
</task>

<task type="auto">
  <name>Task 2: Device and Preferences sections with account page</name>
  <files>
    client/src/components/account/DeviceSection.tsx
    client/src/components/account/PreferencesSection.tsx
    client/src/app/account/page.tsx
  </files>
  <action>
1. Create `client/src/components/account/DeviceSection.tsx`:
   - "use client" directive
   - Uses useQuery for getUserSessions, useMutation for deleteSession
   - Lists all sessions as rows: device name (bold), device type icon (use lucide: Monitor for desktop, Smartphone for mobile, Tablet for tablet), IP address (gray text), last active time (relative: "2 hours ago" using simple helper)
   - "This device" badge on current session (isCurrent: true)
   - "Remove" button on all sessions EXCEPT current. On click: confirm dialog, then call deleteSession(id), invalidate sessions query.
   - Section wrapper: same card style

2. Create `client/src/components/account/PreferencesSection.tsx`:
   - "use client" directive
   - Uses useQuery for getUserProfile (to get current newsletter value)
   - Newsletter toggle: labeled switch (on/off). On change, call updatePreferences({ newsletter: value }) via useMutation. Optimistic update.
   - Auto-renew toggle: labeled switch. Uses getUserSubscription to get current autoRenew. NOTE: This toggle is display-only -- no PATCH endpoint exists for autoRenew. This is an explicit Phase 7 dependency (Phase 7 adds subscription management with PATCH /api/user/subscription endpoint). Show the toggle reflecting current state but disable it with "Available when subscription management launches" tooltip/note.
   - Section wrapper: same card style

3. Create `client/src/app/account/page.tsx`:
   - "use client" directive
   - Page title: "Account Settings" (large heading, pt-24 to clear navbar)
   - Max width container: max-w-2xl mx-auto px-4
   - Renders all 4 sections in order with gap-8 spacing:
     - ProfileSection
     - SubscriptionSection
     - DeviceSection
     - PreferencesSection
  </action>
  <verify>cd client && npx tsc --noEmit</verify>
  <done>Account page renders all 4 sections, devices can be removed, newsletter toggle works</done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` passes
- /account page renders all 4 sections
- Profile edits call PATCH /user/profile
- Device removal calls DELETE /sessions/:id
- Newsletter toggle calls PATCH /user/preferences
- Auto-renew toggle is disabled with Phase 7 dependency note
- No broken imports or missing types
</verification>

<success_criteria>
- Profile shows initials avatar, read-only email, editable name and phone
- Subscription shows plan info or "no active subscription"
- Device list shows all sessions with remove (except current)
- Newsletter toggle is functional, auto-renew toggle is display-only with Phase 7 dependency note
- Single scrollable page, card-style sections
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-player-and-user-features/05-06-SUMMARY.md`
</output>
