---
phase: 05-video-player-and-user-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/services/mylist.service.ts
  - api/src/routes/mylist.routes.ts
  - api/src/services/user.service.ts
  - api/src/validators/user.validators.ts
  - api/src/routes/user.routes.ts
  - api/src/routes/index.ts
autonomous: true

must_haves:
  truths:
    - "User can add content to My List via POST /api/my-list/:contentId"
    - "User can remove content from My List via DELETE /api/my-list/:contentId"
    - "User can check if content is in My List via GET /api/my-list/:contentId"
    - "User can get full My List via GET /api/my-list"
    - "User can view profile via GET /api/user/profile"
    - "User can update display name and phone via PATCH /api/user/profile"
    - "User can update newsletter preference via PATCH /api/user/preferences"
    - "User can view subscription status via GET /api/user/subscription"
  artifacts:
    - path: "api/src/services/mylist.service.ts"
      provides: "My List CRUD using Watchlist model"
      exports: ["getMyList", "isInMyList", "addToMyList", "removeFromMyList"]
    - path: "api/src/services/user.service.ts"
      provides: "User profile, preferences, subscription queries"
      exports: ["getUserProfile", "updateUserProfile", "updatePreferences", "getUserSubscription"]
    - path: "api/src/routes/mylist.routes.ts"
      provides: "My List REST endpoints"
    - path: "api/src/routes/user.routes.ts"
      provides: "User profile/preferences/subscription endpoints"
  key_links:
    - from: "api/src/services/mylist.service.ts"
      to: "prisma.watchlist"
      via: "Watchlist model as My List backing store"
      pattern: "prisma\\.watchlist\\."
    - from: "api/src/routes/index.ts"
      to: "mylist.routes.ts"
      via: "route registration"
      pattern: "/api/my-list"
---

<objective>
Create My List (backed by Watchlist model) and User profile/preferences/subscription backend APIs.

Purpose: Backend for My List toggle feature and Account Settings page.
Output: 7 new API endpoints across 2 route files, registered and compiling.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-video-player-and-user-features/05-CONTEXT.md
@.planning/phases/05-video-player-and-user-features/05-RESEARCH.md
@api/prisma/schema.prisma
@api/src/routes/index.ts
@api/src/middleware/auth.ts
@api/src/services/session.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: My List service and routes</name>
  <files>
    api/src/services/mylist.service.ts
    api/src/routes/mylist.routes.ts
  </files>
  <action>
1. Create `api/src/services/mylist.service.ts`:
   - Uses the existing `Watchlist` model as backing store for "My List" (per RESEARCH.md decision)
   - `getMyList(userId)`: Find all Watchlist entries for user, include Content relation, order by createdAt DESC. Return content objects.
   - `isInMyList(userId, contentId)`: Find unique Watchlist entry. Return boolean.
   - `addToMyList(userId, contentId)`: Upsert (idempotent). Use @@unique([userId, contentId]). Return the entry.
   - `removeFromMyList(userId, contentId)`: Delete where userId+contentId. Use deleteMany (doesn't throw if not found). Return void.
   - Import PrismaClient from `../../generated/prisma/client.js`

2. Create `api/src/routes/mylist.routes.ts`:
   - All routes use requireAuth middleware (router.use(requireAuth))
   - GET `/` — Get full My List. Call getMyList(req.user.id). Return 200 with array.
   - GET `/:contentId` — Check if in list. Call isInMyList(). Return 200 { inList: boolean }.
   - POST `/:contentId` — Add to list. Call addToMyList(). Return 201 { success: true }.
   - DELETE `/:contentId` — Remove from list. Call removeFromMyList(). Return 200 { success: true }.
  </action>
  <verify>npx tsc --noEmit --project api/tsconfig.json</verify>
  <done>My List service and routes compile, 4 endpoints defined</done>
</task>

<task type="auto">
  <name>Task 2: User service, routes, and route registration</name>
  <files>
    api/src/services/user.service.ts
    api/src/validators/user.validators.ts
    api/src/routes/user.routes.ts
    api/src/routes/index.ts
  </files>
  <action>
1. Create `api/src/validators/user.validators.ts`:
   - `updateProfileSchema`: z.object({ name: z.string().min(1).max(100).optional(), phone: z.string() with same phone validation as auth validators (07/01 prefix, transforms to +254) .optional() }). At least one field required: .refine(data => data.name || data.phone).
   - `updatePreferencesSchema`: z.object({ newsletter: z.boolean() })

2. Create `api/src/services/user.service.ts`:
   - `getUserProfile(userId)`: Find user by ID, select id, email, phone, name, role, createdAt. Return user object.
   - `updateUserProfile(userId, data)`: Update user with validated name/phone. For phone, check uniqueness (catch P2002 -> 409 Conflict). Return updated user.
   - `updatePreferences(userId, data)`: Update user newsletter field. Return updated preferences.
   - `getUserSubscription(userId)`: Find active subscription for user (status ACTIVE, order by endDate DESC, take 1). Include Plan relation. Return subscription with plan name, status, endDate, autoRenew. If no active sub, return null.
   - Import PrismaClient from `../../generated/prisma/client.js`

3. Create `api/src/routes/user.routes.ts`:
   - All routes use requireAuth middleware
   - GET `/profile` — Call getUserProfile(req.user.id). Return 200.
   - PATCH `/profile` — Validate body with updateProfileSchema. Call updateUserProfile(). Return 200.
   - PATCH `/preferences` — Validate with updatePreferencesSchema. Call updatePreferences(). Return 200.
   - GET `/subscription` — Call getUserSubscription(). Return 200 (null if no active sub).

4. Update `api/src/routes/index.ts`:
   - Import myListRouter from "./mylist.routes.js"
   - Import userRouter from "./user.routes.js"
   - Register: `app.use("/api/my-list", myListRouter)` and `app.use("/api/user", userRouter)`
   - Add both to barrel exports
  </action>
  <verify>npx tsc --noEmit --project api/tsconfig.json</verify>
  <done>User profile/preferences/subscription endpoints compile, My List and User routes registered in index.ts</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project api/tsconfig.json` passes
- Routes registered: /api/my-list (4 endpoints), /api/user (4 endpoints)
- My List uses Watchlist model (not a new model)
- User subscription returns plan info or null
</verification>

<success_criteria>
- 8 new API endpoints across my-list and user route files
- All endpoints behind requireAuth
- Phone validation reuses existing pattern from auth validators
- Subscription query handles no-active-subscription gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-player-and-user-features/05-02-SUMMARY.md`
</output>
