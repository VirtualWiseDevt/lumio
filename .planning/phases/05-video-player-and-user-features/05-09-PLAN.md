---
phase: 05-video-player-and-user-features
plan: 09
type: execute
wave: 5
depends_on: ["05-08"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Video player plays MP4 content at /watch/[id]"
    - "Player controls auto-hide and keyboard shortcuts work"
    - "My List toggle works from content card, detail modal, and player"
    - "Account settings page renders all 4 sections"
    - "API endpoints return correct data for authenticated requests"
    - "App compiles and runs without errors"
---

<objective>
Verify all Phase 5 features end-to-end: video player, My List, account settings, progress tracking, and Continue Watching.

Purpose: Confirm all requirements (PLAY-01 through PLAY-04, USER-01 through USER-05) are met.
Output: Verified working features or identified gaps.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-video-player-and-user-features/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and API smoke tests</name>
  <files></files>
  <action>
1. Run full build checks:
   - `cd api && npx tsc --noEmit` (API compiles)
   - `cd client && npx tsc --noEmit` (Client compiles)
   - `cd client && npm run build` (Next.js production build succeeds)

2. Start the API server and test endpoints with curl:
   - First, login to get a token: `curl -X POST http://localhost:5000/api/auth/login -H "Content-Type: application/json" -d '{"identifier":"admin@lumio.tv","password":"AdminPass123!"}'`
   - Test progress endpoints:
     - POST /api/progress (save progress)
     - GET /api/progress/continue-watching
   - Test my-list endpoints:
     - GET /api/my-list
     - POST /api/my-list/:contentId (use a known content ID from seed data)
     - GET /api/my-list/:contentId (check returns inList: true)
     - DELETE /api/my-list/:contentId
   - Test user endpoints:
     - GET /api/user/profile
     - PATCH /api/user/profile (update name)
     - PATCH /api/user/preferences (toggle newsletter)
     - GET /api/user/subscription

3. Fix any compilation errors or runtime issues discovered.

4. Verify Next.js routing:
   - Confirm /watch/[id] page exists in build output
   - Confirm /my-list page exists in build output
   - Confirm /account page exists in build output
  </action>
  <verify>All curl commands return expected responses, both tsc --noEmit pass, next build succeeds</verify>
  <done>All API endpoints respond correctly, app builds without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5 implementation:
- Video player with HLS/MP4 support at /watch/[id]
- Netflix-style overlay controls with auto-hide
- Keyboard shortcuts (Space, Arrow keys, F, M, Esc)
- Progress scrubber (red played, gray buffered, dark remaining)
- My List toggle on content cards, detail modal, and player
- /my-list page with content grid
- My List content row on home page
- /account page with Profile, Subscription, Devices, Preferences sections
- Progress tracking (saves every 10s + on unload)
- Continue Watching row on home page
- Next episode auto-advance with 10s countdown
  </what-built>
  <how-to-verify>
1. Start the app: `cd api && npm run dev` and `cd client && npm run dev`
2. Open http://localhost:3000

**Video Player (PLAY-01, PLAY-02, PLAY-03, PLAY-04):**
3. Click Play on any content card or detail modal
4. Verify player opens fullscreen with video (or placeholder if no video URL in seed data)
5. Move mouse -- verify controls appear with play/pause, skip, volume, fullscreen, progress bar
6. Wait 3 seconds -- verify controls auto-hide
7. Press Space (play/pause), Left/Right arrows (skip 10s), F (fullscreen), M (mute), Esc (close)
8. Check progress bar: red played section, scrubber dot on hover, time tooltip
9. Try clicking on the progress bar to seek

**My List (USER-01, USER-02):**
10. Hover over a content card -- verify MyListButton (+ icon) appears in popover
11. Click the + icon -- verify it toggles to checkmark instantly
12. Navigate to /my-list -- verify the content appears in the grid
13. Open a detail modal -- verify MyListButton appears and reflects current state
14. Check home page -- verify "My List" content row appears with added items

**Account Settings (USER-05):**
15. Navigate to /account
16. Verify Profile section shows avatar, email (read-only), name (editable), phone (editable)
17. Verify Subscription section shows plan info or "no subscription"
18. Verify Devices section shows current session
19. Verify Preferences section has newsletter toggle and disabled auto-renew toggle

**Continue Watching (USER-03, USER-04):**
20. After watching some content, navigate to home page
21. Verify Continue Watching row appears with progress bars on cards
22. Click a Continue Watching card -- verify playback resumes from saved position
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- API and client both compile without errors
- Next.js build succeeds
- All API endpoints return expected data
- Human verifies visual and interactive features
</verification>

<success_criteria>
- All 9 requirements verified (PLAY-01 through PLAY-04, USER-01 through USER-05)
- No compilation errors
- No runtime crashes
- User approves the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-player-and-user-features/05-09-SUMMARY.md`
</output>
