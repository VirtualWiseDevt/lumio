---
phase: 05-video-player-and-user-features
plan: 05
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - client/src/hooks/use-my-list.ts
  - client/src/components/my-list/MyListButton.tsx
  - client/src/app/my-list/page.tsx
autonomous: true

must_haves:
  truths:
    - "MyListButton toggles between + and checkmark icons with optimistic updates"
    - "/my-list page displays all content in user's list as a grid"
    - "Toggling My List is instant (optimistic) with rollback on error"
  artifacts:
    - path: "client/src/hooks/use-my-list.ts"
      provides: "My List toggle hook with TanStack Query optimistic updates"
      exports: ["useMyList"]
    - path: "client/src/components/my-list/MyListButton.tsx"
      provides: "Plus/Check toggle button component"
    - path: "client/src/app/my-list/page.tsx"
      provides: "My List page route"
  key_links:
    - from: "client/src/hooks/use-my-list.ts"
      to: "client/src/api/my-list.ts"
      via: "TanStack Query wrapping API calls"
      pattern: "useQuery|useMutation"
    - from: "client/src/components/my-list/MyListButton.tsx"
      to: "client/src/hooks/use-my-list.ts"
      via: "hook consumption"
      pattern: "useMyList"
---

<objective>
Build the My List feature frontend: optimistic toggle hook, button component, and dedicated /my-list page.

Purpose: Users can save content to their personal list from anywhere in the app.
Output: Reusable MyListButton component and /my-list page with grid layout.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-video-player-and-user-features/05-CONTEXT.md
@.planning/phases/05-video-player-and-user-features/05-03-SUMMARY.md
@client/src/api/my-list.ts
@client/src/types/player.ts
@client/src/components/content/ContentCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: useMyList hook with optimistic updates</name>
  <files>
    client/src/hooks/use-my-list.ts
  </files>
  <action>
Create `client/src/hooks/use-my-list.ts`:
- Use TanStack Query (useQuery + useMutation + useQueryClient)
- `useMyList(contentId: string)` hook:
  - useQuery with key ["my-list", contentId] calls isInMyList(contentId) -> returns boolean
  - useMutation for toggle:
    - mutationFn: if currently in list, call removeFromMyList; else call addToMyList
    - onMutate (optimistic): cancelQueries, save previous value, setQueryData to opposite boolean, return { previous }
    - onError: rollback to previous value from context
    - onSettled: invalidateQueries for ["my-list"] (broad invalidation to update the /my-list page)
  - Return { isInList: boolean, toggle: () => void, isLoading: boolean }
- Follow the exact pattern from RESEARCH.md Pattern 5 (optimistic My List toggle)
  </action>
  <verify>cd client && npx tsc --noEmit</verify>
  <done>useMyList hook compiles with optimistic toggle behavior</done>
</task>

<task type="auto">
  <name>Task 2: MyListButton component and /my-list page</name>
  <files>
    client/src/components/my-list/MyListButton.tsx
    client/src/app/my-list/page.tsx
  </files>
  <action>
1. Create `client/src/components/my-list/MyListButton.tsx`:
   - "use client" directive
   - Props: contentId (string), size?: "sm" | "md" | "lg" (default "md"), className?: string
   - Uses useMyList(contentId) hook
   - Renders a button with:
     - Plus icon when NOT in list (lucide-react Plus)
     - Check icon when IN list (lucide-react Check)
     - Circular button with border: border border-white/50 rounded-full
     - Size variants: sm=w-8 h-8, md=w-10 h-10, lg=w-12 h-12
     - onClick calls toggle()
     - Disabled while isLoading (prevent double-tap)
   - Transition: scale on click (active:scale-90), opacity transition on icon swap

2. Create `client/src/app/my-list/page.tsx`:
   - "use client" directive
   - Uses TanStack Query: useQuery with key ["my-list"] calling getMyList()
   - Displays page title "My List" in large text
   - Grid layout: responsive grid (2 cols mobile, 3 tablet, 4 desktop, 6 large)
   - Each item renders ContentCard (from existing components) with the content data
   - Empty state: centered message "Your list is empty. Add movies and series to keep track of what you want to watch."
   - Loading state: skeleton grid (reuse skeleton pattern from browse pages)
   - Use the MyListItem type from types/player.ts
  </action>
  <verify>cd client && npx tsc --noEmit</verify>
  <done>MyListButton renders toggle icon, /my-list page shows grid of saved content</done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` passes
- useMyList returns { isInList, toggle, isLoading }
- MyListButton accepts contentId and renders Plus/Check
- /my-list page uses TanStack Query and shows grid
</verification>

<success_criteria>
- MyListButton is a standalone reusable component (can be placed anywhere)
- Optimistic updates: icon changes instantly, reverts on API error
- /my-list page shows user's saved content in responsive grid
- Empty state handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-player-and-user-features/05-05-SUMMARY.md`
</output>
