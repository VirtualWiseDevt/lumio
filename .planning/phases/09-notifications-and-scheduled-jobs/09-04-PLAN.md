---
phase: 09-notifications-and-scheduled-jobs
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "TypeScript compiles with zero errors across entire api workspace"
    - "Server starts without crashes and all 4 jobs/workers log startup"
    - "Console transport logs email output for all trigger points"
    - "No fire-and-forget email send uses await"
    - "All NOTF requirements have corresponding trigger points wired"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 9 implementation: TypeScript compilation, server startup, email trigger wiring, and requirement coverage.

Purpose: Ensures all notification infrastructure works end-to-end before marking Phase 9 complete.
Output: Verified build with all NOTF-01 through NOTF-06 requirements covered.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-notifications-and-scheduled-jobs/09-CONTEXT.md
@.planning/phases/09-notifications-and-scheduled-jobs/09-01-SUMMARY.md
@.planning/phases/09-notifications-and-scheduled-jobs/09-02-SUMMARY.md
@.planning/phases/09-notifications-and-scheduled-jobs/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build verification and requirement audit</name>
  <files></files>
  <action>
Run comprehensive verification:

1. **TypeScript compilation:**
   ```bash
   cd api && npx tsc --noEmit
   ```
   Must pass with zero errors. Fix any issues found.

2. **Prisma validation:**
   ```bash
   cd api && npx prisma validate
   ```

3. **Server startup test:**
   ```bash
   cd api && timeout 8 npx tsx src/server.ts 2>&1 || true
   ```
   Verify these 4 startup logs appear:
   - `[SESSION_CLEANUP] Scheduled hourly stale session cleanup`
   - `Transcode worker started`
   - `Reconciliation job started`
   - `[EXPIRY_JOB] Scheduled hourly subscription expiry checks`

4. **Requirement coverage audit** -- verify each NOTF requirement has a wired trigger:
   - NOTF-01 (welcome): `grep "buildWelcomeEmail" api/src/services/auth.service.ts`
   - NOTF-02 (payment success): `grep "buildPaymentSuccessEmail" api/src/services/payment.service.ts`
   - NOTF-03 (payment failure): `grep "buildPaymentFailureEmail" api/src/services/payment.service.ts`
   - NOTF-04 (pre-expiry): `grep "buildPreExpiryEmail" api/src/jobs/subscriptionExpiry.job.ts`
   - NOTF-05 (post-expiry): `grep "buildPostExpiryEmail" api/src/jobs/subscriptionExpiry.job.ts`
   - NOTF-06 (referral reward): `grep "buildReferralRewardEmail" api/src/services/referral.service.ts`

5. **Fire-and-forget pattern verification:**
   ```bash
   grep -rn "await sendEmail" api/src/
   ```
   Must return NO results. All sendEmail calls must be fire-and-forget (no await).

6. **Idempotency verification:**
   ```bash
   grep -n "notifiedPreExpiry2Day\|notifiedPreExpiry1Day\|notifiedPostExpiry" api/src/jobs/subscriptionExpiry.job.ts
   ```
   Must show all 3 tracking fields used in queries AND updates.

7. **Reconciliation placeholder removal:**
   ```bash
   grep "RECONCILIATION_NOTIFICATION_PLACEHOLDER" api/src/jobs/reconciliation.job.ts
   ```
   Must return NO results (placeholder fully replaced).

8. **Password reset DEV log removal:**
   ```bash
   grep "DEV.*Password reset token" api/src/services/auth.service.ts
   ```
   Must return NO results (replaced with real email).

If any check fails, fix the issue and re-verify. Document all fixes made.
  </action>
  <verify>All 8 verification checks pass with no issues.</verify>
  <done>Phase 9 fully verified: all NOTF requirements covered, TypeScript compiles, server starts with all jobs, fire-and-forget pattern enforced, idempotent tracking confirmed, legacy placeholders removed.</done>
</task>

</tasks>

<verification>
All checks from Task 1 pass. Zero TypeScript errors. Server starts cleanly. All 6 NOTF requirements mapped to trigger points.
</verification>

<success_criteria>
1. `npx tsc --noEmit` passes with zero errors
2. Server starts with 4 background jobs logging their schedules
3. All 6 NOTF requirements have wired trigger points
4. No `await sendEmail` anywhere (fire-and-forget enforced)
5. Reconciliation placeholder and DEV password reset log are fully replaced
6. Idempotent tracking fields used in expiry cron job
</success_criteria>

<output>
After completion, create `.planning/phases/09-notifications-and-scheduled-jobs/09-04-SUMMARY.md`
</output>
