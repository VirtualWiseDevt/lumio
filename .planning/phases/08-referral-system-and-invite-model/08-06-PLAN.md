---
phase: 08-referral-system-and-invite-model
plan: 06
type: execute
wave: 4
depends_on: ["08-03", "08-04", "08-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "API compiles and all routes respond correctly"
    - "Client and admin panel build without errors"
    - "Registration enforces invite-only via referral code"
    - "Payment flow supports coupon and credit discounts"
    - "Invite Friends page is accessible and functional"
  artifacts: []
  key_links: []
---

<objective>
Build verification and end-to-end functional check of the entire referral system.

Purpose: Confirms all Phase 8 requirements (REF-01 through REF-05, USER-06) are satisfied before marking the phase complete.
Output: Verified working referral system
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-referral-system-and-invite-model/08-CONTEXT.md
@.planning/phases/08-referral-system-and-invite-model/08-01-SUMMARY.md
@.planning/phases/08-referral-system-and-invite-model/08-02-SUMMARY.md
@.planning/phases/08-referral-system-and-invite-model/08-03-SUMMARY.md
@.planning/phases/08-referral-system-and-invite-model/08-04-SUMMARY.md
@.planning/phases/08-referral-system-and-invite-model/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and API smoke tests</name>
  <files></files>
  <action>
Run build checks for all three apps:

1. `cd api && npx tsc --noEmit` -- API must compile
2. `cd client && npx next build` -- Client must build (or `npx tsc --noEmit` if build takes too long)
3. `cd admin && npx tsc --noEmit` -- Admin panel must compile

Run Prisma checks:
4. `cd api && npx prisma validate` -- Schema must be valid
5. `cd api && npx prisma migrate status` -- All migrations applied

API smoke tests (with docker compose up running):
6. `curl -s http://localhost:5000/api/health` -- Health check
7. `curl -s http://localhost:5000/api/referrals/validate/nonexistent` -- Should return 404 with { valid: false }
8. `curl -s http://localhost:5000/api/plans` -- Should return plans array

Fix any compilation errors, type mismatches, or import issues found.
  </action>
  <verify>All three apps compile. Prisma validates. API health check responds 200.</verify>
  <done>All apps compile, schema valid, API responds to health check and referral validation endpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete referral system: invite-only registration, referral credit stacking, coupon redemption, Invite Friends page, admin invite code management, payment flow with discounts</what-built>
  <how-to-verify>
    With the dev server running (docker compose up + npm run dev in client/admin):

    1. **Admin Invite Codes**: Go to admin panel (localhost:3001) > Invite Codes. Generate an invite code. Copy the code.

    2. **Registration with Invite Code**: Open client (localhost:3000/register). Paste the admin invite code. Verify it shows "Invited by Lumio." Fillout form and register. Verify you are redirected to home page.

    3. **Referral Code Generated**: Go to Account page. Click "Invite Friends". Verify your unique referral link is displayed with copy button, WhatsApp/SMS share buttons, referral stats (0 friends, KES 0 credits), and community guidelines.

    4. **Referral Link Flow**: Copy your referral link. Open in incognito. Verify it redirects to /register with code pre-filled. Verify it shows "Invited by [YourName]."

    5. **Billing with Coupon** (if you have a test coupon in DB): Go to /billing. Click "Have a promo code?" Enter code. Verify discount is shown. Select a plan and verify payment modal shows line item breakdown.

    6. **Login**: Log out (clear token from localStorage). Go to /login. Log in with credentials. Verify redirect to home.

    Report any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All three apps compile without errors
- Registration enforces valid referral/invite code
- Invite Friends page displays correctly with all sections
- Payment modal shows coupon/credit line items when applicable
- Admin can generate and manage invite codes
</verification>

<success_criteria>
REF-01: Every user has unique referral code and link -- VERIFIED
REF-02: Registration requires valid referral code (invite-only) -- VERIFIED
REF-03: Referral credit granting on first payment -- VERIFIED (code path exists)
REF-04: Credits deducted at payment time, KES 0 path works -- VERIFIED (code path exists)
REF-05: Invite Friends page with link, copy, guidelines -- VERIFIED
USER-06: Coupon/promo code redemption on billing page -- VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/08-referral-system-and-invite-model/08-06-SUMMARY.md`
</output>
