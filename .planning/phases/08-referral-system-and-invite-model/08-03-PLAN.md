---
phase: 08-referral-system-and-invite-model
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - api/src/routes/admin-invite.routes.ts
  - api/src/services/admin-invite.service.ts
  - api/src/validators/admin-invite.validators.ts
  - api/src/routes/index.ts
  - admin/src/api/invite-codes.ts
  - admin/src/pages/invite-codes.tsx
  - admin/src/routeTree.gen.ts
autonomous: true

must_haves:
  truths:
    - "Admin can generate invite codes with configurable max uses via API"
    - "Admin can list, activate, and deactivate invite codes"
    - "Admin panel has a basic invite codes management page"
  artifacts:
    - path: "api/src/routes/admin-invite.routes.ts"
      provides: "Admin invite code CRUD endpoints"
      exports: ["adminInviteRouter"]
    - path: "api/src/services/admin-invite.service.ts"
      provides: "Invite code generation and management"
      exports: ["createInviteCode", "listInviteCodes", "toggleInviteCode"]
    - path: "admin/src/pages/invite-codes.tsx"
      provides: "Admin UI for managing invite codes"
  key_links:
    - from: "api/src/routes/admin-invite.routes.ts"
      to: "api/src/services/admin-invite.service.ts"
      via: "route handler calls"
      pattern: "createInviteCode|listInviteCodes"
    - from: "admin/src/pages/invite-codes.tsx"
      to: "admin/src/api/invite-codes.ts"
      via: "API client calls"
      pattern: "createInviteCode|listInviteCodes"
---

<objective>
Create admin API for generating and managing invite codes, plus a basic admin panel page for bootstrapping initial users.

Purpose: Enables the invite-only model by letting admins create codes for the first wave of users who then generate their own referral codes.
Output: Admin invite code API + admin panel page
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-referral-system-and-invite-model/08-CONTEXT.md
@.planning/phases/08-referral-system-and-invite-model/08-01-SUMMARY.md
@api/src/routes/index.ts
@api/src/middleware/auth.middleware.ts
@admin/src/routeTree.gen.ts
@admin/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin invite code API (service, validators, routes)</name>
  <files>api/src/services/admin-invite.service.ts, api/src/validators/admin-invite.validators.ts, api/src/routes/admin-invite.routes.ts, api/src/routes/index.ts</files>
  <action>
**admin-invite.service.ts**:

1. `createInviteCode(maxUses: number, createdBy: string)`: Generate code using `crypto.randomBytes(4).toString("hex").toUpperCase()` (8 char uppercase hex). Create AdminInviteCode record. Return the created record.

2. `listInviteCodes()`: Return all AdminInviteCode records ordered by createdAt desc. Include computed field `remainingUses: maxUses - usedCount`.

3. `toggleInviteCode(id: string, isActive: boolean)`: Update AdminInviteCode.isActive. Return updated record.

**admin-invite.validators.ts**:
- `createInviteCodeSchema`: z.object({ maxUses: z.number().int().min(1).max(1000).default(1) })

**admin-invite.routes.ts**:
- Use requireAuth + requireAdmin middleware on all routes.
1. `POST /` -- create invite code. Parse body with createInviteCodeSchema. Call createInviteCode(body.maxUses, req.user.id). Return 201.
2. `GET /` -- list all invite codes. Call listInviteCodes(). Return array.
3. `PATCH /:id` -- toggle active. Parse body `{ isActive: boolean }`. Call toggleInviteCode(id, isActive). Return updated.

Export as `adminInviteRouter`.

**routes/index.ts** -- add:
- `app.use("/api/admin/invite-codes", adminInviteRouter)`

Use ESM imports with .js extensions.
  </action>
  <verify>
Run `cd api && npx tsc --noEmit` -- should compile.
Verify route registration: `grep "adminInviteRouter\|invite-codes" api/src/routes/index.ts`
  </verify>
  <done>Admin invite code API with create, list, toggle endpoints. All behind requireAuth + requireAdmin. Compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Admin panel invite codes management page</name>
  <files>admin/src/api/invite-codes.ts, admin/src/pages/invite-codes.tsx, admin/src/routeTree.gen.ts</files>
  <action>
**admin/src/api/invite-codes.ts** -- API client:
1. `createInviteCode(maxUses: number)`: POST /api/admin/invite-codes with { maxUses }.
2. `listInviteCodes()`: GET /api/admin/invite-codes.
3. `toggleInviteCode(id: string, isActive: boolean)`: PATCH /api/admin/invite-codes/{id} with { isActive }.

Define TypeScript interface `InviteCode { id, code, maxUses, usedCount, isActive, createdAt }`.

**admin/src/pages/invite-codes.tsx** -- simple management page:
- Header: "Invite Codes" with "Generate Code" button
- Generate dialog: number input for max uses (default 1), Generate button
- Table with columns: Code (monospace font, copyable), Max Uses, Used, Remaining, Status (active/inactive badge), Created, Actions (toggle active/inactive)
- Use shadcn Table, Button, Dialog, Input, Badge components
- Use TanStack Query for data fetching and mutations
- Use sonner toast for copy-to-clipboard feedback and creation success
- Copy code on click with navigator.clipboard.writeText()

**admin/src/routeTree.gen.ts** -- add route:
- Register /invite-codes route pointing to invite-codes page
- Follow existing pattern in the manual route tree

Also add a link in the admin sidebar to "Invite Codes" (look at the sidebar component, likely in admin/src/components/layout/ or similar).

Use bundler moduleResolution (no .js extensions on imports). Use @/* path alias if the admin project has one configured, otherwise use relative imports matching existing patterns.
  </action>
  <verify>
Run `cd admin && npx tsc --noEmit` -- should compile.
Verify route registered: `grep "invite-codes" admin/src/routeTree.gen.ts`
  </verify>
  <done>Admin panel has Invite Codes page with generate, list, copy, and toggle functionality. Route registered in sidebar and route tree.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in both api/ and admin/
- Admin can create invite codes via API
- Admin panel shows invite codes page with code management UI
</verification>

<success_criteria>
- Admin invite code API creates codes, lists them, and toggles active status
- Admin panel page renders invite codes table with generate, copy, and toggle actions
- Routes properly registered in both Express app and admin route tree
</success_criteria>

<output>
After completion, create `.planning/phases/08-referral-system-and-invite-model/08-03-SUMMARY.md`
</output>
