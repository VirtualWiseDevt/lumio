---
phase: 08-referral-system-and-invite-model
plan: 05
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - client/src/app/invite/page.tsx
  - client/src/api/referral.ts
  - client/src/app/billing/page.tsx
  - client/src/api/billing.ts
  - client/src/components/billing/PaymentModal.tsx
  - client/src/components/billing/CouponInput.tsx
  - client/src/types/billing.ts
  - client/src/app/account/page.tsx
autonomous: true

must_haves:
  truths:
    - "Invite Friends page shows referral link with copy button and WhatsApp/SMS share buttons"
    - "Invite Friends page shows referral stats (friends joined, credits earned)"
    - "Invite Friends page shows community guidelines with suspension warnings"
    - "Billing page has expandable coupon code input that validates and shows discount"
    - "Payment modal shows line item breakdown (plan price - coupon - credits = final amount)"
    - "Account page has link to Invite Friends page"
    - "Billing page displays current referral credit balance fetched from /api/referrals/stats currentBalance field"
  artifacts:
    - path: "client/src/app/invite/page.tsx"
      provides: "Invite Friends page"
      min_lines: 100
    - path: "client/src/api/referral.ts"
      provides: "Referral API client functions"
      exports: ["getReferralStats", "getMyReferralCode"]
    - path: "client/src/components/billing/CouponInput.tsx"
      provides: "Expandable coupon code input component"
    - path: "client/src/components/billing/PaymentModal.tsx"
      provides: "Modified payment modal with credit/coupon line items"
  key_links:
    - from: "client/src/app/invite/page.tsx"
      to: "/api/referrals/stats"
      via: "TanStack Query"
      pattern: "getReferralStats"
    - from: "client/src/app/billing/page.tsx"
      to: "/api/referrals/stats"
      via: "TanStack Query using getReferralStats"
      pattern: "currentBalance"
    - from: "client/src/components/billing/CouponInput.tsx"
      to: "/api/coupons/validate"
      via: "API call on submit"
      pattern: "validateCoupon"
    - from: "client/src/components/billing/PaymentModal.tsx"
      to: "/api/payments/initiate"
      via: "includes couponCode in request"
      pattern: "couponCode"
---

<objective>
Create the Invite Friends page with referral link sharing and stats, add coupon code input to billing page, and update payment modal to show credit/coupon line items.

Purpose: Completes the client-facing referral and coupon experience -- users can share their code and see discounts at payment time.
Output: /invite page, CouponInput component, modified billing page and payment modal
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-referral-system-and-invite-model/08-CONTEXT.md
@.planning/phases/08-referral-system-and-invite-model/08-02-SUMMARY.md
@client/src/app/billing/page.tsx
@client/src/components/billing/PaymentModal.tsx
@client/src/api/billing.ts
@client/src/types/billing.ts
@client/src/app/account/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invite Friends page with referral link, sharing, and stats</name>
  <files>client/src/app/invite/page.tsx, client/src/api/referral.ts, client/src/app/account/page.tsx</files>
  <action>
**client/src/api/referral.ts** -- API client:
1. `getMyReferralCode()`: GET /api/referrals/my-code. Returns `{ referralCode: string, referralUrl: string }`.
2. `getReferralStats()`: GET /api/referrals/stats. Returns `{ friendsJoined: number, creditsEarned: number, currentBalance: number }`. Note: `currentBalance` is the user's spendable referralCreditBalance (used by the billing page for display and payment calculations).

Import api from "./client".

**client/src/app/invite/page.tsx** -- "use client" page:

Layout per CONTEXT.md decisions (LOCKED -- follow exactly):

1. **Hero section**: heading "Invite Friends, Earn Free Streaming" with subtitle "For every friend who joins and subscribes, you earn 10% off your next payment. Refer 10 friends and stream for free!"

2. **Referral link box**:
   - Display referral URL in a styled box (bg-white/5, border, rounded, monospace or normal text).
   - "Copy" button on the right. On click, copy URL to clipboard using `navigator.clipboard.writeText()`.
   - Show "Copied!" with checkmark icon for 2 seconds after copy, then revert to "Copy".

3. **Share buttons** (side by side):
   - "Share via WhatsApp" -- green button with WhatsApp-style icon. Links to `https://wa.me/?text=${encodedMessage}`. Message: "Join me on Lumio! Stream unlimited movies, series & Live TV. Use my invite link: {referralUrl}". Use `encodeURIComponent`.
   - "Share via SMS" -- button. Links to `sms:?body=${encodedMessage}`. Message: "Join me on Lumio! {referralUrl}".
   - Both open in new tab/window for WhatsApp, SMS uses native handler.

4. **Referral stats**:
   - Two stat cards side by side: "Friends Joined" (count) and "Credits Earned" (KES amount).
   - Fetch via TanStack Query using getReferralStats().

5. **Community Guidelines** section (prominent, not hidden):
   - Exact text from CONTEXT.md: "We kindly remind you that sharing this link on social media platforms including Reddit, Discord, Telegram, Facebook, and Instagram is strictly prohibited. Violations result in: Immediate suspension of your account, Suspension of your referrer's account, Suspension of accounts you referred. Lumio is a privacy-centric service curated for friends and family exclusively."
   - Style with a warning/notice card (border-yellow-500/20 bg, or similar prominent styling).

Data fetching: Use TanStack Query for both referral code and stats. Show skeleton loading states.

**client/src/app/account/page.tsx** -- add Invite Friends link:
- Add a link/button to /invite in the account page. Place it after the existing sections or within ProfileSection area.
- Style: "Invite Friends" with a share/users icon, linking to /invite.
- Could be a card or a prominent link within the existing layout.
  </action>
  <verify>
Run `cd client && npx tsc --noEmit` -- should compile.
Verify page exists: `ls client/src/app/invite/page.tsx`
Verify referral API: `grep "export" client/src/api/referral.ts`
  </verify>
  <done>Invite Friends page with copy-to-clipboard referral link, WhatsApp/SMS share buttons, referral stats, and community guidelines. Account page has Invite Friends link.</done>
</task>

<task type="auto">
  <name>Task 2: Billing page coupon input and payment modal credit/coupon line items</name>
  <files>client/src/components/billing/CouponInput.tsx, client/src/app/billing/page.tsx, client/src/components/billing/PaymentModal.tsx, client/src/api/billing.ts, client/src/types/billing.ts</files>
  <action>
**client/src/types/billing.ts** -- add types:
```
export interface CouponValidation {
  valid: boolean;
  discountPercentage?: number;
  couponId?: string;
}

// Update InitiatePaymentResponse if needed to include discount info
```

**client/src/api/billing.ts** -- add functions:
1. `validateCoupon(code: string)`: POST /api/coupons/validate with { code }. Returns CouponValidation.

Modify the `initiatePayment` function to accept optional `couponCode`:
```
export async function initiatePayment(
  planId: string,
  phone: string,
  couponCode?: string
): Promise<InitiatePaymentResponse> {
  const { data } = await api.post<InitiatePaymentResponse>(
    "/payments/initiate",
    { planId, phone, couponCode }
  );
  return data;
}
```

**client/src/components/billing/CouponInput.tsx** -- new component:
- Expandable: starts as a text link "Have a promo code?"
- On click, expands to show input field + "Apply" button
- On apply: call validateCoupon(code). Show result:
  - Valid: green text "20% discount applied!" with the percentage
  - Invalid: red text with error message
- Props: `onCouponApplied(discountPercentage: number, couponCode: string)` callback
- Props: `onCouponRemoved()` callback
- Show "Remove" link when coupon is applied

**client/src/app/billing/page.tsx** -- modify:
1. Add CouponInput between plan selection and Pay button.
2. Track applied coupon state: `{ code: string, discountPercentage: number } | null`.
3. When coupon is applied, show discounted prices on PlanCards (optional: or just show in the payment modal).
4. **Fetch referral credit balance:** Import `getReferralStats` from `client/src/api/referral.ts`. Use TanStack Query to call `getReferralStats()` and extract the `currentBalance` field. Display "Referral Credits: KES {currentBalance}" below plan selection. This is the spendable balance from the `/api/referrals/stats` endpoint (NOT `creditsEarned`, which is the lifetime total).
5. Pass couponCode and referralCredits (currentBalance) to PaymentModal.

**client/src/components/billing/PaymentModal.tsx** -- modify:
1. Accept new props: `couponCode?: string`, `couponDiscount?: number`, `referralCredits?: number`.
2. In the confirm step, show line item breakdown:
   ```
   Plan: {planName}           KES {planPrice}
   Coupon ({X}%):            -KES {couponDiscount}
   Referral Credits:         -KES {creditsUsed}
   ─────────────────────────────────────
   Total:                     KES {finalAmount}
   ```
   Only show coupon/credit lines if they are > 0.
3. If finalAmount is 0, change button text to "Activate Free" instead of "Pay with M-Pesa".
4. Pass couponCode to initiatePayment call.
5. In the success step, show the actual amount paid (which may be KES 0).

Also: ensure the payment modal's waiting text shows the actual finalAmount, not plan.price.
  </action>
  <verify>
Run `cd client && npx tsc --noEmit` -- should compile.
Verify CouponInput exists: `ls client/src/components/billing/CouponInput.tsx`
Verify PaymentModal accepts coupon props: `grep "couponCode\|couponDiscount\|referralCredits" client/src/components/billing/PaymentModal.tsx`
Verify billing page fetches referral stats: `grep "getReferralStats\|currentBalance" client/src/app/billing/page.tsx`
  </verify>
  <done>Billing page has coupon input, shows referral credit balance from /api/referrals/stats currentBalance field, and payment modal displays line item breakdown with discounts. KES 0 payments show "Activate Free" button.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in client/
- /invite page renders with referral link, share buttons, stats, and guidelines
- Billing page has coupon input that validates codes
- Billing page displays "Referral Credits: KES X" using currentBalance from /api/referrals/stats
- Payment modal shows price breakdown with coupon and credit line items
- KES 0 path shows appropriate UI (no M-Pesa prompt)
</verification>

<success_criteria>
- Invite Friends page matches CONTEXT.md layout exactly (hero, link, share, stats, guidelines)
- Coupon input validates and applies percentage discount
- Billing page fetches and displays referralCreditBalance via getReferralStats().currentBalance
- Payment modal shows correct line items: plan price - coupon - credits = total
- Account page links to Invite Friends
- KES 0 payments activate without M-Pesa
</success_criteria>

<output>
After completion, create `.planning/phases/08-referral-system-and-invite-model/08-05-SUMMARY.md`
</output>
