---
phase: 07-payments-and-subscriptions
plan: 06
type: execute
wave: 4
depends_on: ["07-04", "07-05"]
files_modified:
  - client/src/components/billing/SubscribeGate.tsx
  - client/src/app/watch/[id]/page.tsx
  - client/src/components/content/HoverPopover.tsx
  - client/src/components/detail/DetailModal.tsx
  - client/src/app/layout.tsx
autonomous: false

must_haves:
  truths:
    - "Expired users see a subscribe modal when clicking Play instead of video playback"
    - "SubscribeGate modal offers link to /billing page"
    - "Browsing content remains free (no subscription check on browse pages)"
    - "Build succeeds for both API and client"
    - "Server starts without errors with all new routes and jobs"
  artifacts:
    - path: "client/src/components/billing/SubscribeGate.tsx"
      provides: "Subscribe modal overlay for expired users"
      exports: ["SubscribeGate"]
  key_links:
    - from: "client/src/components/billing/SubscribeGate.tsx"
      to: "client/src/hooks/use-subscription.ts"
      via: "useSubscription for active check"
      pattern: "useSubscription|isActive"
    - from: "client/src/app/watch/[id]/page.tsx"
      to: "client/src/components/billing/SubscribeGate.tsx"
      via: "subscription check before player render"
      pattern: "SubscribeGate|isActive"
---

<objective>
Add subscription enforcement on the client side: a SubscribeGate modal that blocks video playback for expired users with a link to /billing, integrated into the watch page and play actions. Then verify the full build works end-to-end.

Purpose: This is the final piece that completes the payment loop -- users are blocked from watching without a subscription and directed to pay.
Output: SubscribeGate component, updated watch page, build verification.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-payments-and-subscriptions/07-CONTEXT.md
@.planning/phases/07-payments-and-subscriptions/07-04-SUMMARY.md
@.planning/phases/07-payments-and-subscriptions/07-05-SUMMARY.md
@client/src/app/watch/[id]/page.tsx
@client/src/components/content/HoverPopover.tsx
@client/src/components/detail/DetailModal.tsx
@client/src/hooks/use-subscription.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SubscribeGate component and integration into watch page and play actions</name>
  <files>
    client/src/components/billing/SubscribeGate.tsx
    client/src/app/watch/[id]/page.tsx
    client/src/components/content/HoverPopover.tsx
    client/src/components/detail/DetailModal.tsx
  </files>
  <action>
1. Create client/src/components/billing/SubscribeGate.tsx:
   - Props: isOpen, onClose
   - Modal overlay on current page (fixed inset-0, bg-black/70, z-50)
   - Centered card with:
     - Lock or play-circle icon
     - Heading: "Subscribe to continue watching"
     - Subtext: "Get unlimited access to movies, series, and documentaries"
     - Primary button: Link to "/billing" ("View Plans")
     - Secondary button: "Close" (calls onClose)
   - Use AnimatePresence from "motion/react" for fade-in
   - Clicking backdrop closes modal

2. Update client/src/app/watch/[id]/page.tsx:
   - Import useSubscription hook
   - Import SubscribeGate component
   - Before rendering the video player, check subscription status:
     - If useSubscription returns isLoading, show a loading state
     - If !isActive, show SubscribeGate overlay instead of (or on top of) the player
     - If isActive, render player normally
   - The SubscribeGate should overlay the page, not replace it (user sees the page behind the modal)
   - Alternative approach if simpler: redirect to /billing if not subscribed. But per CONTEXT.md, the spec says "show modal overlay on current page" -- so use the modal approach.

3. Update play button behavior in HoverPopover.tsx and DetailModal.tsx:
   - Import useSubscription hook
   - When user clicks Play:
     - If subscription is active: navigate to /watch/[id] as before
     - If subscription is NOT active: show SubscribeGate modal instead of navigating
   - Add state: showSubscribeGate (boolean)
   - Render SubscribeGate conditionally when showSubscribeGate is true
   - This prevents expired users from even reaching the watch page

   Note: Be careful not to break existing functionality. The subscription check is additive -- wrap existing Play onClick handler with the subscription check.

4. Add /billing to the navbar or make it accessible:
   - Check if /billing needs to be added to the client layout or navbar
   - At minimum, ensure /billing is reachable by URL (Next.js file-based routing handles this automatically with the billing/page.tsx from Plan 05)
   - If the navbar has relevant links (like Account), consider adding a "Billing" or "Subscribe" link -- but only if it fits naturally. The account page already has a subscription section that could link to /billing.
  </action>
  <verify>
    Run `npx tsc --noEmit` from client directory -- no type errors.
    Run `npm run build` from client directory -- build succeeds.
  </verify>
  <done>SubscribeGate blocks playback for expired users with modal overlay. Play buttons check subscription before navigating. /billing page is accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete M-Pesa payment and subscription system:
- Three subscription plans (Weekly KES 500, Monthly KES 1,250, Quarterly KES 3,000) seeded in database
- M-Pesa mock service for local development (no external dependencies)
- Billing page at /billing with plan cards, subscription status, payment history
- Three-step payment modal (confirm -> waiting -> result)
- Subscription guard on stream endpoints (403 for expired users)
- SubscribeGate modal overlay on player for expired users
- Reconciliation cron job running every 2 minutes
- Payment routes: GET /api/plans, POST /api/payments/initiate, GET /api/payments/:id/status, GET /api/payments/history
- Callback webhook at POST /api/mpesa/callback (no auth)
  </what-built>
  <how-to-verify>
1. Start the API server: `cd api && npm run dev`
2. Start the client: `cd client && npm run dev`
3. Open http://localhost:3000/billing -- verify:
   - Three plan cards displayed (Weekly, Monthly, Quarterly)
   - Monthly plan highlighted as "Best Value" / recommended
   - If logged in as user without subscription: no status card, plan cards prominent
4. Select a plan and click "Pay with M-Pesa":
   - Payment modal opens with Step 1 (confirm amount and phone)
   - Click pay -- should move to Step 2 (waiting with spinner)
   - Mock service should process in 2-4 seconds
   - Step 3 shows success or failure result
5. After successful payment:
   - Subscription status card should appear at top of billing page
   - Payment appears in history table below
6. Try accessing /watch/[any-content-id] without subscription:
   - Should see SubscribeGate modal overlay
7. Verify API: `curl http://localhost:5000/api/plans` returns 3 plans
8. Verify guard: `curl -H "Authorization: Bearer [token]" http://localhost:5000/api/stream/[id]` returns 403 SUBSCRIPTION_REQUIRED (if no active sub)
9. Check server console for "[RECONCILIATION] Scheduled every 2 minutes" message
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds for both api (tsc) and client (next build)
- Server starts with no errors, reconciliation job scheduled
- /billing page renders correctly
- Payment modal three-step flow works with mock service
- SubscribeGate blocks playback for expired users
- Stream routes return 403 for users without active subscription
</verification>

<success_criteria>
- Billing page displays plans, status (if subscribed), and payment history
- Monthly plan is visually highlighted as recommended
- Payment modal: confirm -> waiting with spinner -> success/failure result
- Mock M-Pesa processes payment in 2-4 seconds locally
- After successful payment, subscription is active and user can access streams
- Expired users see SubscribeGate modal on play attempt
- Reconciliation cron is running and would resolve lost callbacks
</success_criteria>

<output>
After completion, create `.planning/phases/07-payments-and-subscriptions/07-06-SUMMARY.md`
</output>
