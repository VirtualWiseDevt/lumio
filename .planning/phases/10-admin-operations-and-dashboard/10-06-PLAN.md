---
phase: 10-admin-operations-and-dashboard
plan: 06
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - admin/src/routes/_authenticated/settings/index.tsx
  - admin/src/components/settings/MpesaSettings.tsx
  - admin/src/components/settings/GeneralSettings.tsx
  - admin/src/components/settings/PricingSettings.tsx
  - admin/src/components/settings/LimitSettings.tsx
  - admin/src/routes/_authenticated/activity-logs/index.tsx
  - admin/src/components/activity-logs/ActivityLogTable.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can configure M-Pesa Daraja API credentials with a test connection button"
    - "Admin can manage general settings (site name, support email)"
    - "Admin can configure pricing (weekly, monthly, quarterly prices)"
    - "Admin can configure device and invite limits"
    - "M-Pesa secrets are masked on read (last 4 chars only)"
    - "Admin can view a paginated, filterable audit trail of all admin CRUD operations"
    - "Activity log shows timestamp, admin name, action, entity type, and details"
  artifacts:
    - path: "admin/src/routes/_authenticated/settings/index.tsx"
      provides: "Settings page with tabbed sections"
      min_lines: 40
    - path: "admin/src/components/settings/MpesaSettings.tsx"
      provides: "M-Pesa credential config with test button"
      exports: ["MpesaSettings"]
    - path: "admin/src/components/settings/GeneralSettings.tsx"
      provides: "General settings form"
      exports: ["GeneralSettings"]
    - path: "admin/src/components/settings/PricingSettings.tsx"
      provides: "Pricing configuration form"
      exports: ["PricingSettings"]
    - path: "admin/src/components/settings/LimitSettings.tsx"
      provides: "Device and invite limit config"
      exports: ["LimitSettings"]
    - path: "admin/src/routes/_authenticated/activity-logs/index.tsx"
      provides: "Activity logs page with audit trail"
      min_lines: 50
    - path: "admin/src/components/activity-logs/ActivityLogTable.tsx"
      provides: "Activity log data table"
      exports: ["ActivityLogTable"]
  key_links:
    - from: "admin/src/components/settings/MpesaSettings.tsx"
      to: "admin/src/api/settings.ts"
      via: "useMutation for updateSettings + testMpesaConnection"
      pattern: "updateSettings|testMpesaConnection"
    - from: "admin/src/routes/_authenticated/activity-logs/index.tsx"
      to: "admin/src/api/activity-logs.ts"
      via: "useQuery for listActivityLogs"
      pattern: "listActivityLogs"
---

<objective>
Build the Settings page (ADMN-11) with M-Pesa configuration, general settings, pricing, and device/invite limits. Build the Activity Logs page (ADMN-12) with a filterable audit trail table. Run final build verification.

Purpose: Completes all Phase 10 admin pages -- settings for platform configuration and activity logs for operational audit trail.
Output: 2 page routes + 5 components
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-admin-operations-and-dashboard/10-RESEARCH.md
@.planning/phases/10-admin-operations-and-dashboard/10-02-SUMMARY.md
@.planning/phases/10-admin-operations-and-dashboard/10-03-SUMMARY.md
@admin/src/api/settings.ts
@admin/src/api/activity-logs.ts
@admin/src/routes/_authenticated/settings/categories.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page with M-Pesa, general, pricing, and limits sections</name>
  <files>
    admin/src/routes/_authenticated/settings/index.tsx
    admin/src/components/settings/MpesaSettings.tsx
    admin/src/components/settings/GeneralSettings.tsx
    admin/src/components/settings/PricingSettings.tsx
    admin/src/components/settings/LimitSettings.tsx
  </files>
  <action>
1. Create admin/src/components/settings/MpesaSettings.tsx:
   - Self-fetching: useQuery for getSettings(["mpesa.consumerKey", "mpesa.consumerSecret", "mpesa.shortcode", "mpesa.passkey", "mpesa.callbackUrl", "mpesa.environment"]).
   - Form with react-hook-form: Consumer Key, Consumer Secret, Shortcode, Passkey, Callback URL, Environment (select: mock/sandbox/production).
   - Pre-fill form with fetched values (masked secrets show "****xxxx" pattern).
   - "Test Connection" button: calls testMpesaConnection(), shows success/error toast.
   - "Save" button: calls updateSettings with form values, shows success toast.
   - Sensitive fields (consumerKey, consumerSecret, passkey) show masked values. When user clears and types new value, that gets submitted. If field still shows masked value (unchanged), skip it in the update payload.
   - Wrapped in Card with "M-Pesa Daraja API" title and description "Configure your Safaricom Daraja API credentials for M-Pesa payments."

2. Create admin/src/components/settings/GeneralSettings.tsx:
   - Self-fetching: getSettings(["general.siteName", "general.supportEmail"]).
   - Form: Site Name (text input), Support Email (email input).
   - "Save" button: updateSettings, toast on success.
   - Wrapped in Card with "General Settings" title.

3. Create admin/src/components/settings/PricingSettings.tsx:
   - Self-fetching: getSettings(["pricing.weeklyPrice", "pricing.monthlyPrice", "pricing.quarterlyPrice"]).
   - Form: Weekly Price (KES, number input), Monthly Price (KES, number input), Quarterly Price (KES, number input).
   - "Save" button: updateSettings, toast on success.
   - Wrapped in Card with "Pricing Configuration" title and note "These values are for reference. Plan prices are managed in the database seed."

4. Create admin/src/components/settings/LimitSettings.tsx:
   - Self-fetching: getSettings(["limits.maxDevices", "limits.maxInviteCodes"]).
   - Form: Max Devices per User (number input, current default 2), Max Invite Codes (number input).
   - "Save" button: updateSettings, toast on success.
   - Wrapped in Card with "Limits" title.

5. Replace admin/src/routes/_authenticated/settings/index.tsx placeholder:
   - PageContainer title="Settings".
   - Layout: vertical stack of setting sections with gap-6:
     - MpesaSettings
     - Separator
     - GeneralSettings
     - Separator
     - PricingSettings
     - Separator
     - LimitSettings
   - Add a link/button at bottom to go to "/settings/categories" for category management (keep existing categories route working).
  </action>
  <verify>Run `cd admin && npx tsc --noEmit` -- no type errors.</verify>
  <done>Settings page shows M-Pesa config with test button, general settings, pricing config, and device/invite limits. All forms save via API.</done>
</task>

<task type="auto">
  <name>Task 2: Activity logs page and build verification</name>
  <files>
    admin/src/routes/_authenticated/activity-logs/index.tsx
    admin/src/components/activity-logs/ActivityLogTable.tsx
  </files>
  <action>
1. Create admin/src/components/activity-logs/ActivityLogTable.tsx:
   - Props: data (ActivityLogEntry[]), sorting/onSortingChange.
   - Column defs: Timestamp (formatted date-time with date-fns format), Admin (user.name or "System"), Action (badge: CREATE=green, UPDATE=blue, DELETE=red, LOGIN=purple, SETTINGS_CHANGE=yellow), Entity Type (badge), Entity ID (truncated), Details (JSON stringified, truncated to 100 chars with tooltip for full), IP Address.
   - Uses @tanstack/react-table with manualSorting.
   - Renders shadcn Table.

2. Replace admin/src/routes/_authenticated/activity-logs/index.tsx placeholder:
   - State: action filter (all/CREATE/UPDATE/DELETE/LOGIN/SETTINGS_CHANGE), entityType filter (all/USER/CONTENT/PAYMENT/SETTINGS), dateRange, page, sortBy, sortOrder.
   - TanStack Query: useQuery for listActivityLogs with filters.
   - Layout inside PageContainer title="Activity Logs":
     - Toolbar: action filter Select, entity type filter Select, date range inputs, search by admin name (optional).
     - ActivityLogTable.
     - Pagination.

3. Run full build verification:
   - `cd api && npx tsc --noEmit` -- API compiles
   - `cd admin && npx tsc --noEmit` -- Admin compiles
   - `cd admin && npm run build` -- Admin builds successfully
   - Verify all route files exist and are registered in routeTree.gen.ts
   - Verify all new sidebar links point to valid routes
  </action>
  <verify>
Run `cd api && npx tsc --noEmit` -- no type errors.
Run `cd admin && npm run build` -- build succeeds.
Verify all 6 new admin pages are accessible (dashboard updated, users, billing, settings, activity-logs all have route files).
  </verify>
  <done>Activity logs page shows filterable audit trail table. Full project builds clean. All Phase 10 pages exist and render.</done>
</task>

</tasks>

<verification>
- `cd api && npx tsc --noEmit` passes
- `cd admin && npm run build` succeeds
- Settings page: M-Pesa config with test button, general settings, pricing, limits
- Activity logs page: filterable audit trail table with action, entity type, date filtering
- All Phase 10 routes registered and accessible via sidebar
- ADMN-02 through ADMN-12 (Phase 10 scope) requirements addressed
</verification>

<success_criteria>
- ADMN-11: Settings page with M-Pesa credentials, test button, general settings, pricing config, device/invite limits
- ADMN-12: Activity logs page with audit trail table (timestamp, admin, action, details) filterable by action and entity type
- Full project compiles and builds without errors
- All 7 Phase 10 requirements (ADMN-02, 03, 04, 09, 10, 11, 12) have corresponding UI pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-and-dashboard/10-06-SUMMARY.md`
</output>
