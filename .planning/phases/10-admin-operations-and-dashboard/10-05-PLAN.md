---
phase: 10-admin-operations-and-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - admin/src/routes/_authenticated/users/index.tsx
  - admin/src/components/users/UserStatsCards.tsx
  - admin/src/components/users/UserTable.tsx
  - admin/src/components/users/UserForm.tsx
  - admin/src/routes/_authenticated/billing/index.tsx
  - admin/src/components/billing/BillingStatsCards.tsx
  - admin/src/components/billing/PaymentTable.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can view users in a paginated, sortable, filterable table"
    - "Admin can add a new user via form dialog"
    - "Admin can edit and delete (deactivate) users"
    - "Admin can view a user's active sessions and terminate them"
    - "Admin can export users to CSV"
    - "Admin can view payments in a filterable, paginated table"
    - "Admin can filter payments by status and user"
    - "Admin can export payments to CSV"
  artifacts:
    - path: "admin/src/routes/_authenticated/users/index.tsx"
      provides: "Users management page"
      min_lines: 60
    - path: "admin/src/components/users/UserTable.tsx"
      provides: "TanStack Table for users"
      exports: ["UserTable"]
    - path: "admin/src/components/users/UserForm.tsx"
      provides: "Add/edit user dialog"
      exports: ["UserForm"]
    - path: "admin/src/routes/_authenticated/billing/index.tsx"
      provides: "Billing management page"
      min_lines: 50
    - path: "admin/src/components/billing/PaymentTable.tsx"
      provides: "TanStack Table for payments"
      exports: ["PaymentTable"]
  key_links:
    - from: "admin/src/routes/_authenticated/users/index.tsx"
      to: "admin/src/api/users.ts"
      via: "TanStack Query useQuery/useMutation"
      pattern: "useQuery.*listUsers|useMutation.*createUser"
    - from: "admin/src/routes/_authenticated/billing/index.tsx"
      to: "admin/src/api/billing.ts"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*listPayments|getBillingStats"
    - from: "admin/src/routes/_authenticated/users/index.tsx"
      to: "admin/src/lib/csv-export.ts"
      via: "exportToCsv call on button click"
      pattern: "exportToCsv"
---

<objective>
Build the Users management page (ADMN-09) and Billing management page (ADMN-10) with stats cards, data tables, filtering, and CSV export.

Purpose: Admin needs full user lifecycle management and billing visibility.
Output: 2 page routes + 5 components
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-admin-operations-and-dashboard/10-RESEARCH.md
@.planning/phases/10-admin-operations-and-dashboard/10-02-SUMMARY.md
@.planning/phases/10-admin-operations-and-dashboard/10-03-SUMMARY.md
@admin/src/api/users.ts
@admin/src/api/billing.ts
@admin/src/lib/csv-export.ts
@admin/src/components/shared/FilterBar.tsx
@admin/src/components/shared/Pagination.tsx
@admin/src/routes/_authenticated/movies/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Users management page with stats, table, form, sessions, CSV</name>
  <files>
    admin/src/routes/_authenticated/users/index.tsx
    admin/src/components/users/UserStatsCards.tsx
    admin/src/components/users/UserTable.tsx
    admin/src/components/users/UserForm.tsx
  </files>
  <action>
1. Create admin/src/components/users/UserStatsCards.tsx:
   - Props: totalUsers (number), activeUsers (number), adminUsers (number), loading (boolean).
   - Renders 3 StatCard components (reuse from dashboard/StatCard.tsx) in a grid: Total Users (Users icon), Active Users (UserCheck icon), Admin Users (Shield icon).
   - No period comparison needed here (just current values).

2. Create admin/src/components/users/UserTable.tsx:
   - Props: data (AdminUser[]), sorting/onSortingChange for TanStack Table manualSorting, onEdit (user => void), onDelete (user => void), onViewSessions (user => void).
   - Column defs: Name, Email, Phone, Role (badge), Status (active/inactive badge), Sessions count, Payments count, Created (formatted date), Actions (dropdown: Edit, View Sessions, Delete).
   - Uses @tanstack/react-table with manualSorting.
   - Renders shadcn Table component.
   - Role badge: ADMIN = purple, USER = default.
   - Status badge: active = green, inactive = red.

3. Create admin/src/components/users/UserForm.tsx:
   - Props: user (AdminUser | null for create), open (boolean), onClose, onSuccess.
   - Dialog with react-hook-form + Zod validation.
   - Fields: Name (required), Email (required, email format), Phone (required, Kenyan format), Password (required for create, optional for edit), Role (select: USER/ADMIN), Active (switch, edit only).
   - Uses useMutation for createUser or updateUser based on whether user prop is provided.
   - Shows toast on success/error via sonner.
   - Calls onSuccess after mutation settles to close dialog and refetch.

4. Replace admin/src/routes/_authenticated/users/index.tsx placeholder:
   - State: search (debounced 300ms), status filter (all/active/inactive), page, sortBy, sortOrder, editUser (null or user), showForm (boolean), showSessions (user or null).
   - TanStack Query: useQuery for listUsers, useQuery for user stats (derive from list response or separate count queries), useMutation for deleteUser.
   - Layout inside PageContainer title="Users":
     - Row 1: UserStatsCards (total, active, admin counts).
     - Row 2: Toolbar with search input, status filter Select, "Add User" Button, "Export CSV" Button.
     - Row 3: UserTable with sorting.
     - Row 4: Pagination.
   - CSV export: on click, call exportUsers with current filters, then exportToCsv with columns [Name, Email, Phone, Role, Status, Created].
   - Sessions dialog: when "View Sessions" clicked, show Dialog listing user's sessions (device, IP, last active) with "Terminate" button per session. Uses getUserSessions and deleteUserSession API calls.
   - Delete: AlertDialog confirmation before calling deleteUser mutation.
   - UserForm dialog for add/edit.
  </action>
  <verify>Run `cd admin && npx tsc --noEmit` -- no type errors.</verify>
  <done>Users page shows stats cards, searchable/filterable table, add/edit form, session viewer, CSV export.</done>
</task>

<task type="auto">
  <name>Task 2: Billing management page with stats, payment table, CSV</name>
  <files>
    admin/src/routes/_authenticated/billing/index.tsx
    admin/src/components/billing/BillingStatsCards.tsx
    admin/src/components/billing/PaymentTable.tsx
  </files>
  <action>
1. Create admin/src/components/billing/BillingStatsCards.tsx:
   - Props: stats (BillingStats from API type), loading (boolean).
   - Renders 4 StatCard components: Total Revenue (DollarSign, format KES), Successful Payments (CheckCircle, green), Failed Payments (XCircle, red), Pending Payments (Clock, yellow).
   - Each shows current value and percentage change vs previous period.

2. Create admin/src/components/billing/PaymentTable.tsx:
   - Props: data (AdminPayment[]), sorting/onSortingChange, loading (boolean).
   - Column defs: User (name + email), Plan, Amount (KES formatted), Discount, Status (badge: SUCCESS=green, FAILED=red, PENDING=yellow, EXPIRED=gray), Method, M-Pesa Receipt, Phone, Date (formatted).
   - Uses @tanstack/react-table with manualSorting.
   - Status badges use StatusBadge component or inline badge styling.

3. Replace admin/src/routes/_authenticated/billing/index.tsx placeholder:
   - State: status filter (all/PENDING/SUCCESS/FAILED/EXPIRED), search (user name/email), dateRange (start/end), page, sortBy, sortOrder.
   - TanStack Query: useQuery for getBillingStats, useQuery for listPayments.
   - Layout inside PageContainer title="Billing":
     - Row 1: BillingStatsCards.
     - Row 2: Toolbar with search input, status filter Select, date range inputs (optional), "Export CSV" Button.
     - Row 3: PaymentTable.
     - Row 4: Pagination.
   - CSV export: call exportPayments with current filters, then exportToCsv with columns [User, Email, Plan, Amount, Discount, Status, Method, Receipt, Phone, Date].
  </action>
  <verify>
Run `cd admin && npx tsc --noEmit` -- no type errors.
Run `cd admin && npm run build` -- build succeeds.
  </verify>
  <done>Billing page shows stats cards, filterable payment table with status/user/date filtering, and CSV export.</done>
</task>

</tasks>

<verification>
- `cd admin && npm run build` succeeds
- Users page: stats cards, searchable table, add/edit/delete, session monitoring, CSV export
- Billing page: stats cards, filterable table, CSV export
- Both pages follow existing admin panel patterns (PageContainer + toolbar + table + pagination)
</verification>

<success_criteria>
- ADMN-09: Users page with stats, table, filters, add/edit/delete, session monitoring, CSV export
- ADMN-10: Billing page with stats, payment history table, status/user filtering, CSV export
- Zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-and-dashboard/10-05-SUMMARY.md`
</output>
