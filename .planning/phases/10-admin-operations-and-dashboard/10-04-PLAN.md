---
phase: 10-admin-operations-and-dashboard
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - admin/src/routes/_authenticated/index.tsx
  - admin/src/components/dashboard/StatCard.tsx
  - admin/src/components/dashboard/RevenueChart.tsx
  - admin/src/components/dashboard/ContentChart.tsx
  - admin/src/components/dashboard/ActivityFeed.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard displays 4 stat cards: revenue, active users, total content, failed payments with percentage change"
    - "Dashboard displays revenue bar chart with 6/12 month toggle"
    - "Dashboard displays content breakdown donut chart by type"
    - "Dashboard displays recent activity feed with period filter (week/month/quarter/all/custom)"
  artifacts:
    - path: "admin/src/routes/_authenticated/index.tsx"
      provides: "Full dashboard page"
      min_lines: 50
    - path: "admin/src/components/dashboard/StatCard.tsx"
      provides: "Reusable stat card with period comparison"
      exports: ["StatCard"]
    - path: "admin/src/components/dashboard/RevenueChart.tsx"
      provides: "Revenue bar chart with month toggle"
      exports: ["RevenueChart"]
    - path: "admin/src/components/dashboard/ContentChart.tsx"
      provides: "Content breakdown donut chart"
      exports: ["ContentChart"]
    - path: "admin/src/components/dashboard/ActivityFeed.tsx"
      provides: "Recent activity feed with period filter"
      exports: ["ActivityFeed"]
  key_links:
    - from: "admin/src/routes/_authenticated/index.tsx"
      to: "admin/src/api/dashboard.ts"
      via: "TanStack Query useQuery calls"
      pattern: "useQuery.*getDashboardStats|getRevenueChart|getContentBreakdown"
    - from: "admin/src/components/dashboard/RevenueChart.tsx"
      to: "recharts"
      via: "BarChart component"
      pattern: "BarChart|ChartContainer"
---

<objective>
Build the admin dashboard page with stat cards, revenue bar chart, content donut chart, and activity feed. Covers ADMN-02, ADMN-03, ADMN-04.

Purpose: Primary admin landing page providing operational visibility at a glance.
Output: Dashboard page + 4 dashboard components
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-admin-operations-and-dashboard/10-RESEARCH.md
@.planning/phases/10-admin-operations-and-dashboard/10-02-SUMMARY.md
@.planning/phases/10-admin-operations-and-dashboard/10-03-SUMMARY.md
@admin/src/api/dashboard.ts
@admin/src/routes/_authenticated/index.tsx
@admin/src/components/layout/PageContainer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard components (StatCard, RevenueChart, ContentChart)</name>
  <files>
    admin/src/components/dashboard/StatCard.tsx
    admin/src/components/dashboard/RevenueChart.tsx
    admin/src/components/dashboard/ContentChart.tsx
  </files>
  <action>
1. Create admin/src/components/dashboard/StatCard.tsx:
   - Props: title (string), value (string | number), change (number, percentage), changeLabel (string, e.g. "vs last month"), icon (LucideIcon), loading (boolean).
   - Renders shadcn Card with: icon top-right, large value, small title, change indicator (green arrow up for positive, red arrow down for negative, gray dash for zero).
   - Format large numbers: use Intl.NumberFormat for KES currency values.
   - Show Skeleton when loading.

2. Create admin/src/components/dashboard/RevenueChart.tsx:
   - Props: data (Array<{ month: string, revenue: number }>), loading (boolean).
   - Internal state: months toggle (6 or 12). Two small buttons "6M" and "12M" in card header.
   - Uses shadcn ChartContainer + Recharts BarChart.
   - Config: { revenue: { label: "Revenue (KES)", color: "hsl(var(--chart-1))" } }.
   - XAxis: month names. YAxis: tickFormatter showing "1K", "10K" etc.
   - ChartTooltip with ChartTooltipContent for hover details.
   - Wrapped in shadcn Card with CardHeader "Revenue" title and CardContent.
   - Show Skeleton when loading.
   - Pass `months` state up via onMonthsChange callback prop so parent can refetch.

3. Create admin/src/components/dashboard/ContentChart.tsx:
   - Props: data (Array<{ type: string, count: number }>), loading (boolean).
   - Uses shadcn ChartContainer + Recharts PieChart with Pie component (innerRadius=60 for donut).
   - Config: movies/series/documentaries/channels each with distinct hsl(var(--chart-N)) colors.
   - Map data to include `fill` property matching config colors.
   - ChartTooltip + ChartLegend for labels.
   - Wrapped in shadcn Card with "Content Breakdown" title.
   - Show Skeleton when loading.

Import Recharts components: Bar, BarChart, XAxis, YAxis, CartesianGrid, Pie, PieChart.
Import shadcn chart: ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent.
  </action>
  <verify>Run `cd admin && npx tsc --noEmit` -- no type errors.</verify>
  <done>StatCard renders metric with change indicator. RevenueChart renders bar chart with month toggle. ContentChart renders donut chart.</done>
</task>

<task type="auto">
  <name>Task 2: ActivityFeed component and dashboard page assembly</name>
  <files>
    admin/src/components/dashboard/ActivityFeed.tsx
    admin/src/routes/_authenticated/index.tsx
  </files>
  <action>
1. Create admin/src/components/dashboard/ActivityFeed.tsx:
   - Props: none (self-fetching component).
   - Internal state: period (week/month/quarter/all/custom), customDateRange (startDate, endDate), page.
   - Uses TanStack Query useQuery to fetch getRecentActivity(params).
   - Period selector: shadcn Select with options "This Week", "This Month", "This Quarter", "All Time", "Custom Range".
   - When "Custom Range" selected, show two date inputs (startDate, endDate) below the select.
   - Renders activity items as a list:
     - Each item: icon (based on action type), description (e.g. "Admin created user john@example.com"), timestamp (formatted with date-fns formatDistanceToNow).
     - Action type icons: UserPlus for CREATE, Pencil for UPDATE, Trash2 for DELETE, LogIn for LOGIN, Settings for SETTINGS_CHANGE.
     - Format description: "{user.name} {action.toLowerCase()} {entityType.toLowerCase()} {entityId || ''}".
   - Pagination at bottom if total > limit.
   - Wrapped in shadcn Card with "Recent Activity" title.
   - Show Skeleton list when loading.

2. Replace admin/src/routes/_authenticated/index.tsx dashboard page:
   - Remove the placeholder content.
   - Use TanStack Query:
     - `useQuery({ queryKey: ["dashboard-stats", periodDays], queryFn: () => getDashboardStats(periodDays) })`
     - `useQuery({ queryKey: ["revenue-chart", months], queryFn: () => getRevenueChart(months) })`
     - `useQuery({ queryKey: ["content-breakdown"], queryFn: () => getContentBreakdown() })`
   - State: periodDays (default 30), months (default 6).
   - Layout inside PageContainer title="Dashboard":
     - Row 1: 4 StatCards in a responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4).
       - Revenue: DollarSign icon, format as "KES {value.toLocaleString()}"
       - Active Users: Users icon
       - Total Content: Film icon
       - Failed Payments: AlertTriangle icon, use destructive color hint
     - Row 2: 2-column grid (lg:grid-cols-7 gap-4). RevenueChart (col-span-4) + ContentChart (col-span-3).
     - Row 3: ActivityFeed (full width).
   - Pass loading states to components for skeleton rendering.
  </action>
  <verify>
Run `cd admin && npx tsc --noEmit` -- no type errors.
Run `cd admin && npm run build` -- build succeeds.
  </verify>
  <done>Dashboard page shows 4 stat cards with period comparison, revenue bar chart with 6/12M toggle, content donut chart, and activity feed with period filtering.</done>
</task>

</tasks>

<verification>
- `cd admin && npm run build` succeeds
- Dashboard page renders stat cards, charts, and activity feed
- Revenue chart has 6M/12M toggle
- Activity feed has period filter with week/month/quarter/all/custom options
- All components show loading skeletons before data arrives
</verification>

<success_criteria>
- ADMN-02: Stat cards show revenue, active users, total content, failed payments with period comparison
- ADMN-03: Revenue bar chart (6/12 month) and content breakdown donut chart visible
- ADMN-04: Activity feed with period filtering (week/month/quarter/all/custom)
- Zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-and-dashboard/10-04-SUMMARY.md`
</output>
