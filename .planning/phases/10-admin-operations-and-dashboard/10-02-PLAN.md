---
phase: 10-admin-operations-and-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - api/src/routes/admin-dashboard.routes.ts
  - api/src/routes/admin-user.routes.ts
  - api/src/routes/admin-billing.routes.ts
  - api/src/routes/admin-settings.routes.ts
  - api/src/routes/admin-activity.routes.ts
  - api/src/routes/index.ts
autonomous: true

must_haves:
  truths:
    - "GET /api/admin/dashboard/stats returns revenue, activeUsers, totalContent, failedPayments with period comparison"
    - "GET /api/admin/dashboard/revenue-chart returns monthly revenue data"
    - "GET /api/admin/dashboard/content-breakdown returns content counts by type"
    - "GET /api/admin/dashboard/activity returns paginated activity feed"
    - "CRUD endpoints at /api/admin/users work with filtering, pagination, session management"
    - "GET /api/admin/billing/stats and /api/admin/billing/payments return billing data"
    - "GET/PUT /api/admin/settings read and write platform settings"
    - "POST /api/admin/settings/mpesa/test tests M-Pesa connection"
    - "GET /api/admin/activity-logs returns filtered, paginated activity logs"
  artifacts:
    - path: "api/src/routes/admin-dashboard.routes.ts"
      provides: "Dashboard API endpoints"
      exports: ["adminDashboardRouter"]
    - path: "api/src/routes/admin-user.routes.ts"
      provides: "User management API endpoints"
      exports: ["adminUserRouter"]
    - path: "api/src/routes/admin-billing.routes.ts"
      provides: "Billing management API endpoints"
      exports: ["adminBillingRouter"]
    - path: "api/src/routes/admin-settings.routes.ts"
      provides: "Settings API endpoints"
      exports: ["adminSettingsRouter"]
    - path: "api/src/routes/admin-activity.routes.ts"
      provides: "Activity logs API endpoint"
      exports: ["adminActivityRouter"]
    - path: "api/src/routes/index.ts"
      provides: "Route registration for all new admin routes"
  key_links:
    - from: "api/src/routes/admin-dashboard.routes.ts"
      to: "api/src/services/dashboard.service.ts"
      via: "service function calls"
      pattern: "getDashboardStats|getRevenueChart|getContentBreakdown|getRecentActivity"
    - from: "api/src/routes/index.ts"
      to: "all new route files"
      via: "registerRoutes"
      pattern: "app\\.use.*admin"
---

<objective>
Create all API route files for Phase 10 admin features and register them in the route index. Each route file uses requireAuth + requireAdmin middleware and calls the services from Plan 01.

Purpose: Frontend plans (03-06) need working API endpoints to fetch data from.
Output: 5 route files + updated route index
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-admin-operations-and-dashboard/10-RESEARCH.md
@.planning/phases/10-admin-operations-and-dashboard/10-01-SUMMARY.md
@api/src/routes/index.ts
@api/src/routes/content.routes.ts
@api/src/routes/admin-invite.routes.ts
@api/src/middleware/auth.middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard and user management routes</name>
  <files>
    api/src/routes/admin-dashboard.routes.ts
    api/src/routes/admin-user.routes.ts
  </files>
  <action>
1. Create api/src/routes/admin-dashboard.routes.ts:
   - `export const adminDashboardRouter = Router();`
   - Apply `router.use(requireAuth, requireAdmin)` at top.
   - GET `/stats` -- parse query with dashboardStatsQuerySchema, call getDashboardStats(periodDays), return JSON.
   - GET `/revenue-chart` -- parse query with revenueChartQuerySchema, call getRevenueChart(months), return JSON.
   - GET `/content-breakdown` -- call getContentBreakdown(), return JSON.
   - GET `/activity` -- parse query with activityFeedQuerySchema, call getRecentActivity(params), return JSON.
   - Follow existing route error handling pattern: safeParse, 422 on validation failure, throw on unexpected errors (Express 5 auto-catches).

2. Create api/src/routes/admin-user.routes.ts:
   - `export const adminUserRouter = Router();`
   - Apply `router.use(requireAuth, requireAdmin)` at top.
   - GET `/` -- parse query with userQuerySchema, call listUsers(params), return JSON.
   - GET `/export` -- parse query with userQuerySchema (minus page/limit), call exportUsers(params), return JSON array.
   - GET `/:userId` -- call getUser(id), 404 if not found.
   - POST `/` -- parse body with createUserSchema, call createUser(data), call logActivity with action "CREATE", entityType "USER", return 201.
   - PUT `/:userId` -- parse body with updateUserSchema, call updateUser(id, data), call logActivity with action "UPDATE", entityType "USER", return JSON.
   - DELETE `/:userId` -- call deleteUser(id), call logActivity with action "DELETE", entityType "USER", return 204.
   - GET `/:userId/sessions` -- call listUserSessions(userId), return JSON.
   - DELETE `/:userId/sessions/:sessionId` -- call deleteUserSession(userId, sessionId), return 204.
   - logActivity calls use req.user.id for userId, req.ip for ipAddress. Fire-and-forget (no await, .catch(() => {})).

ESM imports with .js extensions. Use `req.params` with type assertion for Express 5 params.
  </action>
  <verify>Run `cd api && npx tsc --noEmit` -- no type errors.</verify>
  <done>Dashboard routes return stats/charts/activity. User routes support full CRUD with activity logging.</done>
</task>

<task type="auto">
  <name>Task 2: Billing, settings, activity routes and registration</name>
  <files>
    api/src/routes/admin-billing.routes.ts
    api/src/routes/admin-settings.routes.ts
    api/src/routes/admin-activity.routes.ts
    api/src/routes/index.ts
  </files>
  <action>
1. Create api/src/routes/admin-billing.routes.ts:
   - `export const adminBillingRouter = Router();`
   - Apply `router.use(requireAuth, requireAdmin)` at top.
   - GET `/stats` -- parse query with billingStatsQuerySchema, call getBillingStats(periodDays), return JSON.
   - GET `/payments` -- parse query with billingQuerySchema, call listPayments(params), return JSON.
   - GET `/payments/export` -- parse query with billingQuerySchema (minus page/limit), call exportPayments(params), return JSON array.

2. Create api/src/routes/admin-settings.routes.ts:
   - `export const adminSettingsRouter = Router();`
   - Apply `router.use(requireAuth, requireAdmin)` at top.
   - GET `/` -- parse query with settingKeysSchema, call getSettings(keys), return JSON. If no keys, return all settings.
   - PUT `/` -- parse body with updateSettingsSchema, call setSettings(settings, req.user.id), call logActivity with action "SETTINGS_CHANGE", entityType "SETTINGS", details containing changed keys. Return JSON.
   - POST `/mpesa/test` -- call testMpesaConnection(), return JSON { success, message }.

3. Create api/src/routes/admin-activity.routes.ts:
   - `export const adminActivityRouter = Router();`
   - Apply `router.use(requireAuth, requireAdmin)` at top.
   - GET `/` -- parse query with activityQuerySchema, call listActivityLogs(params), return JSON.

4. Update api/src/routes/index.ts:
   - Import all 5 new routers: adminDashboardRouter, adminUserRouter, adminBillingRouter, adminSettingsRouter, adminActivityRouter.
   - Add to exports object.
   - In registerRoutes, add:
     ```
     app.use("/api/admin/dashboard", adminDashboardRouter);
     app.use("/api/admin/users", adminUserRouter);
     app.use("/api/admin/billing", adminBillingRouter);
     app.use("/api/admin/settings", adminSettingsRouter);
     app.use("/api/admin/activity-logs", adminActivityRouter);
     ```

ESM imports with .js extensions throughout.
  </action>
  <verify>
Run `cd api && npx tsc --noEmit` -- no type errors.
Run `cd api && npm run dev` (or `node dist/server.js` after build) and verify server starts without errors. Check that hitting `curl http://localhost:5000/api/admin/dashboard/stats` returns 401 (auth required).
  </verify>
  <done>All 5 route files created and registered. Server starts without errors. All admin endpoints require authentication.</done>
</task>

</tasks>

<verification>
- `cd api && npx tsc --noEmit` passes
- Server starts without errors
- All new routes are registered under /api/admin/* paths
- Unauthenticated requests to new endpoints return 401
</verification>

<success_criteria>
- 5 route files created with all endpoints per requirements
- Routes registered in index.ts
- Activity logging integrated into mutation endpoints (user CRUD, settings changes)
- Server compiles and starts clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-and-dashboard/10-02-SUMMARY.md`
</output>
