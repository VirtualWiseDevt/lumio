---
phase: 10-admin-operations-and-dashboard
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - admin/src/api/dashboard.ts
  - admin/src/api/users.ts
  - admin/src/api/billing.ts
  - admin/src/api/settings.ts
  - admin/src/api/activity-logs.ts
  - admin/src/lib/csv-export.ts
  - admin/src/components/layout/Sidebar.tsx
  - admin/src/routeTree.gen.ts
  - admin/package.json
autonomous: true

must_haves:
  truths:
    - "Admin API client functions exist for all Phase 10 endpoints"
    - "CSV export utility can convert any array of objects to downloadable CSV"
    - "Sidebar shows Users, Billing, Settings, Activity Logs nav items"
    - "shadcn chart component installed with Recharts"
    - "Route tree updated with new page routes"
  artifacts:
    - path: "admin/src/api/dashboard.ts"
      provides: "Dashboard API client"
      exports: ["getDashboardStats", "getRevenueChart", "getContentBreakdown", "getRecentActivity"]
    - path: "admin/src/api/users.ts"
      provides: "User management API client"
      exports: ["listUsers", "getUser", "createUser", "updateUser", "deleteUser", "getUserSessions", "deleteUserSession", "exportUsers"]
    - path: "admin/src/lib/csv-export.ts"
      provides: "CSV export utility"
      exports: ["exportToCsv"]
    - path: "admin/src/components/layout/Sidebar.tsx"
      provides: "Updated sidebar with new nav items"
  key_links:
    - from: "admin/src/api/dashboard.ts"
      to: "/api/admin/dashboard/*"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*admin/dashboard"
    - from: "admin/src/components/layout/Sidebar.tsx"
      to: "admin/src/routes/_authenticated/*"
      via: "navItems href"
      pattern: "/users|/billing|/settings|/activity-logs"
---

<objective>
Set up all admin panel infrastructure for Phase 10: API client functions, CSV export utility, shadcn chart installation, sidebar navigation updates, and route tree registration.

Purpose: Dashboard and management page plans (04-06) need API clients, chart components, and registered routes to build on.
Output: 5 API client files + CSV utility + updated sidebar + route tree + chart component installed
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-admin-operations-and-dashboard/10-RESEARCH.md
@admin/src/api/client.ts
@admin/src/api/content.ts
@admin/src/components/layout/Sidebar.tsx
@admin/src/routeTree.gen.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install chart component and create API clients + CSV utility</name>
  <files>
    admin/package.json
    admin/src/api/dashboard.ts
    admin/src/api/users.ts
    admin/src/api/billing.ts
    admin/src/api/settings.ts
    admin/src/api/activity-logs.ts
    admin/src/lib/csv-export.ts
  </files>
  <action>
1. Install shadcn chart component:
   ```bash
   cd admin && npx shadcn@latest add chart --yes
   ```
   If recharts has React 19 peer dep issues, add to admin/package.json overrides:
   ```json
   "overrides": { "react-is": "^19.0.0" }
   ```
   Then re-run `npm install`.

2. Create admin/src/api/dashboard.ts:
   - Types: DashboardStats (revenue/activeUsers/totalContent/failedPayments each with current/previous/change), RevenueChartData (Array of { month, revenue }), ContentBreakdown (Array of { type, count }), ActivityFeedItem ({ id, action, entityType, entityId, details, ipAddress, createdAt, user: { name, email } | null }), ActivityFeedResponse ({ data, total, page, totalPages }).
   - Functions using apiClient:
     - `getDashboardStats(periodDays?: number)` -- GET /api/admin/dashboard/stats
     - `getRevenueChart(months?: number)` -- GET /api/admin/dashboard/revenue-chart
     - `getContentBreakdown()` -- GET /api/admin/dashboard/content-breakdown
     - `getRecentActivity(params)` -- GET /api/admin/dashboard/activity

3. Create admin/src/api/users.ts:
   - Types: AdminUser ({ id, name, email, phone, role, isActive, createdAt, _count: { sessions, payments } }), UserListResponse, UserDetail (full user with sessions), UserSession ({ id, deviceName, deviceType, ipAddress, lastActiveAt }), CreateUserInput, UpdateUserInput.
   - Functions: listUsers, getUser, createUser, updateUser, deleteUser, getUserSessions, deleteUserSession, exportUsers.

4. Create admin/src/api/billing.ts:
   - Types: BillingStats (totalRevenue/successfulPayments/failedPayments/pendingPayments with current/previous/change), AdminPayment ({ id, amount, discount, status, method, phoneNumber, mpesaReceiptNumber, createdAt, user: { name, email }, plan: { name } }), PaymentListResponse.
   - Functions: getBillingStats, listPayments, exportPayments.

5. Create admin/src/api/settings.ts:
   - Types: PlatformSettings (Record<string, unknown>), MpesaTestResult ({ success, message }).
   - Functions: getSettings(keys?), updateSettings(settings), testMpesaConnection.

6. Create admin/src/api/activity-logs.ts:
   - Types: ActivityLogEntry ({ id, action, entityType, entityId, details, ipAddress, createdAt, user }), ActivityLogResponse.
   - Functions: listActivityLogs(params).

7. Create admin/src/lib/csv-export.ts:
   - `exportToCsv<T>(data: T[], columns: { key: keyof T; header: string }[], filename: string)` -- generates CSV string from data array, creates Blob, triggers download via temporary anchor element. Escape values containing commas, quotes, or newlines. Filename format: `{filename}-{YYYY-MM-DD}.csv`.
   - Follow the exact pattern from 10-RESEARCH.md code example.

All API client files follow the existing pattern in admin/src/api/content.ts: import apiClient from ./client, export typed async functions.
  </action>
  <verify>Run `cd admin && npx tsc --noEmit` -- no type errors. Verify recharts is installed: `ls admin/node_modules/recharts`.</verify>
  <done>All API client files export typed functions. CSV utility exports exportToCsv. Recharts + shadcn chart components installed.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar navigation and route tree updates</name>
  <files>
    admin/src/components/layout/Sidebar.tsx
    admin/src/routeTree.gen.ts
  </files>
  <action>
1. Update admin/src/components/layout/Sidebar.tsx:
   - Add new nav items to the navItems array (add after "Invite Codes"):
     - { to: "/users", label: "Users", icon: Users } (import Users from lucide-react)
     - { to: "/billing", label: "Billing", icon: CreditCard } (import CreditCard from lucide-react)
     - { to: "/activity-logs", label: "Activity Logs", icon: ScrollText } (import ScrollText from lucide-react)
   - Update the existing Settings item from `{ to: "/settings/categories", label: "Settings", icon: Settings }` to `{ to: "/settings", label: "Settings", icon: Settings }`.
   - Keep the existing anchor tag navigation pattern (not TanStack Link).
   - Organize nav items into logical groups with Separator between content management and operations:
     - Group 1: Dashboard, Movies, Series, Documentaries, TV Channels
     - Separator
     - Group 2: Users, Billing, Activity Logs
     - Separator
     - Group 3: Settings, Invite Codes

2. Update admin/src/routeTree.gen.ts:
   - Import new route components (will be created in Plans 04-06, use placeholder imports):
     - usersRoute from "./routes/_authenticated/users/index"
     - billingRoute from "./routes/_authenticated/billing/index"
     - settingsRoute from "./routes/_authenticated/settings/index"
     - activityLogsRoute from "./routes/_authenticated/activity-logs/index"
   - Add to authenticatedRoute.addChildren([...existing, usersRoute, billingRoute, settingsRoute, activityLogsRoute]).
   - IMPORTANT: These route files don't exist yet (created in Plans 04-06). Create minimal placeholder route files so the route tree compiles:
     - admin/src/routes/_authenticated/users/index.tsx -- export usersRoute with path "/users", component renders PageContainer with "Users" title and "Coming soon" text.
     - admin/src/routes/_authenticated/billing/index.tsx -- export billingRoute, path "/billing", placeholder.
     - admin/src/routes/_authenticated/settings/index.tsx -- export settingsRoute, path "/settings", placeholder. This REPLACES the current categories-only settings page. Keep the categories route registered separately.
     - admin/src/routes/_authenticated/activity-logs/index.tsx -- export activityLogsRoute, path "/activity-logs", placeholder.
   - Each placeholder follows the exact pattern of the existing dashboardRoute in _authenticated/index.tsx.
  </action>
  <verify>
Run `cd admin && npx tsc --noEmit` -- no type errors.
Run `cd admin && npm run build` -- build succeeds.
  </verify>
  <done>Sidebar shows all new nav items grouped logically. Route tree includes all new routes. Placeholder pages render without errors. Admin panel builds clean.</done>
</task>

</tasks>

<verification>
- `cd admin && npx tsc --noEmit` passes
- `cd admin && npm run build` succeeds
- Sidebar has Users, Billing, Activity Logs, and updated Settings nav items
- Route tree includes all new routes
- Recharts and shadcn chart component installed
</verification>

<success_criteria>
- 5 API client files with typed functions matching all backend endpoints
- CSV export utility ready for use
- shadcn chart component installed (Recharts available)
- Sidebar updated with operational nav items
- Route tree updated and compiles with placeholder pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-and-dashboard/10-03-SUMMARY.md`
</output>
