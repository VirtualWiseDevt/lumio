---
phase: 04-client-browsing-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/tsconfig.json
  - client/next.config.ts
  - client/postcss.config.mjs
  - client/src/app/globals.css
  - client/src/app/layout.tsx
  - client/src/app/page.tsx
  - client/src/app/@modal/default.tsx
  - client/src/components/providers.tsx
  - client/src/components/layout/Navbar.tsx
  - client/src/api/client.ts
  - client/src/types/content.ts
  - client/src/lib/utils.ts
  - client/src/stores/ui.ts
autonomous: true

must_haves:
  truths:
    - "Next.js 15 dev server starts at localhost:3000 without errors"
    - "API requests from client proxy to localhost:5000 in development"
    - "Dark theme is applied globally with consistent color scheme"
    - "Navbar renders with Lumio logo and navigation links"
    - "TanStack Query provider wraps the application"
    - "TypeScript types match the Content, Season, Episode Prisma models"
  artifacts:
    - path: "client/package.json"
      provides: "Next.js 15 with all dependencies"
      contains: "next"
    - path: "client/src/app/layout.tsx"
      provides: "Root layout with providers, navbar, and @modal slot"
      min_lines: 20
    - path: "client/src/components/providers.tsx"
      provides: "TanStack Query provider"
      contains: "QueryClientProvider"
    - path: "client/src/components/layout/Navbar.tsx"
      provides: "Site navigation bar"
      min_lines: 20
    - path: "client/src/api/client.ts"
      provides: "Axios instance for API calls"
      contains: "axios.create"
    - path: "client/src/types/content.ts"
      provides: "TypeScript types for Content, Season, Episode, BrowseRow"
      contains: "ContentType"
    - path: "client/src/stores/ui.ts"
      provides: "Zustand store for UI state"
      contains: "useUIStore"
  key_links:
    - from: "client/src/app/layout.tsx"
      to: "client/src/components/providers.tsx"
      via: "Providers wrapper component"
      pattern: "<Providers>"
    - from: "client/next.config.ts"
      to: "localhost:5000"
      via: "API proxy rewrites"
      pattern: "rewrites|destination.*5000"
---

<objective>
Scaffold the Next.js 15 client application with all dependencies, configuration, providers, navigation, API client, TypeScript types, and Zustand store.

Purpose: Establish the client app foundation that all subsequent browsing UI plans build on. Includes the dark theme, layout with @modal parallel route slot, and all utility code.
Output: A running Next.js 15 dev server at localhost:3000 with dark theme, navbar, TanStack Query, Zustand, and API proxy.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-browsing-experience/04-RESEARCH.md
@admin/src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure Next.js 15</name>
  <files>client/package.json, client/tsconfig.json, client/next.config.ts, client/postcss.config.mjs, client/src/app/globals.css</files>
  <action>
1. Delete the existing skeleton client/package.json. Create a new one:
```json
{
  "name": "@lumio/client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "start": "next start --port 3000",
    "lint": "next lint"
  }
}
```

2. Install from the client/ directory:
```bash
npm install next@15.5.9 react@^19 react-dom@^19
npm install tailwindcss@^4 @tailwindcss/postcss postcss
npm install @tanstack/react-query@^5 zustand@^5 motion@^12 axios@^1
npm install clsx tailwind-merge class-variance-authority lucide-react
npm install -D typescript@^5.9 @types/react @types/react-dom
```

3. Create `client/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "plugins": [{ "name": "next" }]
  },
  "include": ["src", "next-env.d.ts", ".next/types/**/*.ts"],
  "exclude": ["node_modules", ".next"]
}
```

4. Create `client/next.config.ts`:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  async rewrites() {
    return [
      {
        source: "/api/:path*",
        destination: "http://localhost:5000/api/:path*",
      },
    ];
  },
  images: {
    remotePatterns: [
      {
        protocol: "http",
        hostname: "localhost",
        port: "5000",
        pathname: "/api/media/**",
      },
    ],
  },
};

export default nextConfig;
```

5. Create `client/postcss.config.mjs`:
```javascript
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```

6. Create `client/src/app/globals.css` with Tailwind v4 CSS-first config:
```css
@import "tailwindcss";

@theme {
  --color-background: #0a0a0a;
  --color-foreground: #e5e5e5;
  --color-card: #141414;
  --color-card-hover: #1a1a1a;
  --color-border: #262626;
  --color-muted: #737373;
  --color-accent: #e50914;
  --color-accent-hover: #f40612;
  --font-sans: "Inter", "Helvetica Neue", Arial, sans-serif;
}

html {
  color-scheme: dark;
}

body {
  background-color: var(--color-background);
  color: var(--color-foreground);
  font-family: var(--font-sans);
}

/* Scrollbar hiding for content rows */
.scrollbar-hide {
  scrollbar-width: none;
}
.scrollbar-hide::-webkit-scrollbar {
  display: none;
}
```

IMPORTANT: Tailwind v4 uses CSS-first config with @theme directive. No tailwind.config.js/ts file needed.
  </action>
  <verify>Run `npm run dev` from client/ directory -- Next.js dev server should start at localhost:3000 without errors.</verify>
  <done>Next.js 15.5.9 dev server runs at localhost:3000. All dependencies installed. Tailwind 4 configured with dark theme. API proxy rewrites active.</done>
</task>

<task type="auto">
  <name>Task 2: Create root layout, providers, navbar, types, API client, and Zustand store</name>
  <files>
    client/src/app/layout.tsx
    client/src/app/page.tsx
    client/src/app/@modal/default.tsx
    client/src/components/providers.tsx
    client/src/components/layout/Navbar.tsx
    client/src/api/client.ts
    client/src/types/content.ts
    client/src/lib/utils.ts
    client/src/stores/ui.ts
  </files>
  <action>
Create all foundational files:

**1. `client/src/lib/utils.ts`** - Copy the cn() utility pattern from admin:
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

export function formatDuration(minutes: number): string {
  const h = Math.floor(minutes / 60);
  const m = minutes % 60;
  if (h === 0) return `${m}m`;
  return m === 0 ? `${h}h` : `${h}h ${m}m`;
}

export function mediaUrl(path: string | null | undefined): string {
  if (!path) return "";
  return `/api/media/${path}`;
}
```

**2. `client/src/types/content.ts`** - TypeScript types matching the Prisma schema:
```typescript
export type ContentType = "MOVIE" | "SERIES" | "DOCUMENTARY" | "CHANNEL";

export interface Content {
  id: string;
  type: ContentType;
  title: string;
  description: string | null;
  releaseYear: number | null;
  duration: number | null;
  ageRating: string | null;
  quality: string | null;
  categories: string[];
  cast: string[];
  director: string | null;
  posterPortrait: string | null;
  posterLandscape: string | null;
  trailerUrl: string | null;
  streamUrl: string | null;
  isFeatured: boolean;
  createdAt: string;
  updatedAt: string;
}

export interface Season {
  id: string;
  contentId: string;
  number: number;
  title: string | null;
  episodes: Episode[];
}

export interface Episode {
  id: string;
  seasonId: string;
  number: number;
  title: string;
  description: string | null;
  duration: number | null;
  videoUrl: string | null;
  thumbnail: string | null;
}

export interface ContentDetail extends Content {
  seasons?: Season[];
}

export interface BrowseRow {
  title: string;
  slug: string;
  items: Content[];
}

export interface BrowsePageData {
  featured: Content[];
  rows: BrowseRow[];
}

export interface LiveTvData {
  categories: {
    name: string;
    channels: Content[];
  }[];
}

export interface SearchResults {
  movies: Content[];
  series: Content[];
  documentaries: Content[];
  channels: Content[];
}
```

**3. `client/src/api/client.ts`** - Axios instance:
```typescript
import axios from "axios";

export const api = axios.create({
  baseURL: "/api",
  headers: { "Content-Type": "application/json" },
});
```

**4. `client/src/stores/ui.ts`** - Zustand store for UI state:
```typescript
import { create } from "zustand";

interface UIState {
  isSearchOpen: boolean;
  openSearch: () => void;
  closeSearch: () => void;
  toggleSearch: () => void;
}

export const useUIStore = create<UIState>((set) => ({
  isSearchOpen: false,
  openSearch: () => set({ isSearchOpen: true }),
  closeSearch: () => set({ isSearchOpen: false }),
  toggleSearch: () => set((state) => ({ isSearchOpen: !state.isSearchOpen })),
}));
```

**5. `client/src/components/providers.tsx`** - TanStack Query provider:
```tsx
"use client";

import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useState } from "react";

export function Providers({ children }: { children: React.ReactNode }) {
  const [queryClient] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            staleTime: 60 * 1000,
            refetchOnWindowFocus: false,
          },
        },
      })
  );

  return (
    <QueryClientProvider client={queryClient}>
      {children}
    </QueryClientProvider>
  );
}
```

**6. `client/src/components/layout/Navbar.tsx`** - Navigation bar:
A dark, semi-transparent sticky navbar with:
- Lumio logo/text on the left (bold, accent color for "L")
- Navigation links: Home, Movies, Series, Documentaries, Live TV
- Search icon button on the right (clicking calls useUIStore.openSearch)
- Use `"use client"` directive (needs Zustand store access)
- Use `next/link` for navigation links
- Active link detection with `usePathname()` from `next/navigation`
- Semi-transparent background with backdrop-blur (sticky top-0 z-50)

**7. `client/src/app/layout.tsx`** - Root layout:
```tsx
import type { Metadata } from "next";
import { Providers } from "@/components/providers";
import { Navbar } from "@/components/layout/Navbar";
import "./globals.css";

export const metadata: Metadata = {
  title: "Lumio - Stream Movies, Series & Live TV",
  description: "Premium video streaming platform for East Africa",
};

export default function RootLayout({
  children,
  modal,
}: {
  children: React.ReactNode;
  modal: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className="min-h-screen antialiased">
        <Providers>
          <Navbar />
          <main>{children}</main>
          {modal}
        </Providers>
      </body>
    </html>
  );
}
```

**8. `client/src/app/page.tsx`** - Placeholder home page:
```tsx
export default function Home() {
  return (
    <div className="flex items-center justify-center min-h-[80vh]">
      <p className="text-muted text-lg">Home page — coming in Plan 07</p>
    </div>
  );
}
```

**9. `client/src/app/@modal/default.tsx`** - Parallel route default (required for intercepting routes):
```tsx
export default function ModalDefault() {
  return null;
}
```
  </action>
  <verify>
Run `npm run dev` from client/ and visit http://localhost:3000:
- Page loads with dark background
- Navbar shows with Lumio branding and navigation links
- No console errors
- Search icon visible in navbar
  </verify>
  <done>Client app renders with dark theme, navbar with all navigation links, TanStack Query provider active, Zustand store available, API client configured, TypeScript types defined, @modal parallel route slot ready for intercepting routes.</done>
</task>

</tasks>

<verification>
- `npm run dev` from client/ starts without errors at localhost:3000
- `npm run build` from client/ completes without TypeScript errors
- Dark theme renders correctly
- Navbar displays all navigation links (Home, Movies, Series, Documentaries, Live TV)
- API proxy works: client requests to /api/* reach Express at localhost:5000
</verification>

<success_criteria>
- Next.js 15.5.9 running at localhost:3000 with all dependencies
- Dark theme with Netflix-inspired color scheme
- Navbar with navigation links and search icon
- TanStack Query provider wrapping the app
- Zustand store for UI state (search open/close)
- API client configured with proxy to Express backend
- TypeScript types for Content, Season, Episode, BrowsePageData, SearchResults
- @modal parallel route slot configured for future intercepting routes
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-browsing-experience/04-02-SUMMARY.md`
</output>
