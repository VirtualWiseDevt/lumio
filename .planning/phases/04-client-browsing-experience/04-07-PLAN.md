---
phase: 04-client-browsing-experience
plan: 07
type: execute
wave: 3
depends_on: ["04-03", "04-04", "04-05", "04-06"]
files_modified:
  - client/src/app/page.tsx
  - client/src/app/movies/page.tsx
  - client/src/app/series/page.tsx
  - client/src/app/documentaries/page.tsx
  - client/src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Home page displays hero banner with featured content and content rows below"
    - "Movies page displays hero banner with featured movies and movie-specific content rows"
    - "Series page displays hero banner with featured series and series-specific content rows"
    - "Documentaries page displays hero banner with featured documentaries and documentary-specific content rows"
    - "Content cards in rows are clickable and open the detail modal"
    - "Hover popover works on content cards across all pages"
    - "All pages fetch data via TanStack Query from the browse API"
    - "Navbar links navigate between pages"
  artifacts:
    - path: "client/src/app/page.tsx"
      provides: "Home page with hero banner and content rows"
      min_lines: 30
    - path: "client/src/app/movies/page.tsx"
      provides: "Movies browse page"
      min_lines: 20
    - path: "client/src/app/series/page.tsx"
      provides: "Series browse page"
      min_lines: 20
    - path: "client/src/app/documentaries/page.tsx"
      provides: "Documentaries browse page"
      min_lines: 20
  key_links:
    - from: "client/src/app/page.tsx"
      to: "client/src/api/content.ts"
      via: "TanStack Query fetch"
      pattern: "fetchHomePageData"
    - from: "client/src/app/page.tsx"
      to: "client/src/components/hero/HeroBanner.tsx"
      via: "renders hero banner"
      pattern: "<HeroBanner"
    - from: "client/src/app/page.tsx"
      to: "client/src/components/content/ContentRow.tsx"
      via: "renders content rows"
      pattern: "<ContentRow"
    - from: "client/src/app/movies/page.tsx"
      to: "client/src/api/content.ts"
      via: "TanStack Query fetch"
      pattern: "fetchBrowsePageData.*movies"
---

<objective>
Wire all components together into the four main browse pages: Home, Movies, Series, and Documentaries. Each page fetches data from the browse API and renders a hero banner with content rows.

Purpose: This plan connects all the individual components (hero banner, content rows, cards, popover, detail modal) into working pages. It's the integration layer that makes the browsing experience functional.
Output: Four complete browse pages that fetch real data and render the full Netflix-style UI.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-client-browsing-experience/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Home page with hero banner and content rows</name>
  <files>client/src/app/page.tsx, client/src/app/layout.tsx</files>
  <action>
**Replace `client/src/app/page.tsx`** with the complete home page:

`"use client"` component (needs TanStack Query hooks and event handlers).

Implementation:
1. Use TanStack Query: `queryKey: ["home"], queryFn: fetchHomePageData`
2. Render `<HeroBanner items={data.featured} />` at the top (full-width, no padding)
3. Below the hero, render content rows section:
   - Vertical spacing: mt-[-4rem] relative (rows slightly overlap the hero bottom gradient -- Netflix style) or just mt-8 if simpler
   - For each row in `data.rows`: render `<ContentRow title={row.title} items={row.items} />`
   - Pass an `onCardClick` handler that navigates to `/title/{content.id}` using `router.push` from `next/navigation` -- this triggers the intercepting route modal
4. Loading state: full-page skeleton with:
   - Hero placeholder: aspect-video bg-card animate-pulse
   - 3 row placeholders: title skeleton + 6 card skeletons in a row
5. Error state: centered error message with retry button
6. Empty state (no featured content): show a message "No content available yet" with a subtle illustration or icon

Also update `client/src/app/layout.tsx` if needed:
- Ensure SearchOverlay is imported and rendered (should have been done in Plan 06 Task 1, but verify)
- Ensure the layout structure is clean: Navbar, main > children, modal, SearchOverlay

The `onCardClick` on ContentRow should use Next.js `useRouter().push()` to trigger client-side navigation. This will activate the intercepting route and show the detail modal overlay.
  </action>
  <verify>
Start both the API server (localhost:5000) and client (localhost:3000).
- Visit localhost:3000 -- home page loads
- If content exists in DB (from admin seeding), hero banner shows and content rows render
- If no content, empty state displays
- Click a content card -- should navigate to /title/{id} and show modal
  </verify>
  <done>Home page renders hero banner with featured content and admin-configured content rows. Cards are clickable and open the detail modal via intercepting routes.</done>
</task>

<task type="auto">
  <name>Task 2: Build Movies, Series, and Documentaries pages</name>
  <files>client/src/app/movies/page.tsx, client/src/app/series/page.tsx, client/src/app/documentaries/page.tsx</files>
  <action>
Create three browse pages that follow the same pattern. Each is a `"use client"` component.

All three pages share the same structure (DRY by extracting a shared component if desired, or just keep them simple and direct):

**Pattern for each page:**
1. Use TanStack Query: `queryKey: ["browse", type], queryFn: () => fetchBrowsePageData(type)`
   - Movies: type = "movies"
   - Series: type = "series"
   - Documentaries: type = "documentaries"
2. Render `<HeroBanner items={data.featured} />` (featured items of this content type)
3. Render content rows below the hero (same pattern as home page)
4. Pass `onCardClick` to navigate to `/title/{id}`
5. Loading state: hero skeleton + row skeletons
6. Error state: error message with retry

**Option (recommended): Create a shared `BrowsePage` component:**

`client/src/components/pages/BrowsePage.tsx` -- `"use client"` component.
Props: `{ type: "movies" | "series" | "documentaries" }`

This component encapsulates the shared logic:
- TanStack Query fetch with the type parameter
- HeroBanner + ContentRow rendering
- Loading/error states

Then each page file is minimal:
```tsx
// client/src/app/movies/page.tsx
"use client";
import { BrowsePage } from "@/components/pages/BrowsePage";
export default function MoviesPage() {
  return <BrowsePage type="movies" />;
}
```

Same for series and documentaries. This avoids duplicating the layout/fetch logic.

If creating the shared component, also create `client/src/components/pages/BrowsePage.tsx` and add it to files_modified.
  </action>
  <verify>
- Visit localhost:3000/movies -- shows movies hero + rows
- Visit localhost:3000/series -- shows series hero + rows
- Visit localhost:3000/documentaries -- shows documentaries hero + rows
- Navbar links navigate between pages
- Cards are clickable and open detail modal
- Content is filtered by type (movies page only shows movies, etc.)
  </verify>
  <done>Movies, Series, and Documentaries pages each render a hero banner with type-specific featured content and filtered content rows. Navigation between pages works via navbar links. Cards open the detail modal.</done>
</task>

</tasks>

<verification>
- All four browse pages load and display data from the browse API
- Hero banner renders on each page with type-appropriate featured content
- Content rows scroll horizontally with snap behavior
- Hover popover appears after 500ms on content cards
- Clicking a card opens the detail modal via intercepting routes
- Navbar navigation works between all pages
- Loading and error states render cleanly
- No console errors or hydration mismatches
</verification>

<success_criteria>
- Home page: hero banner + admin-configured content rows from /api/browse
- Movies page: hero + movie-filtered rows from /api/browse/movies
- Series page: hero + series-filtered rows from /api/browse/series
- Documentaries page: hero + documentary-filtered rows from /api/browse/documentaries
- All interactive features working: hero rotation, content row scrolling, hover popover, detail modal
- Navbar links navigate between all pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-browsing-experience/04-07-SUMMARY.md`
</output>
