---
phase: 03-content-api-and-admin-content-management
plan: 08
type: execute
wave: 4
depends_on: ["03-05", "03-06"]
files_modified:
  - admin/src/routes/_authenticated/series/index.tsx
  - admin/src/routes/_authenticated/series/new.tsx
  - admin/src/routes/_authenticated/series/$seriesId.tsx
  - admin/src/routes/_authenticated/series/$seriesId.seasons.$seasonId.tsx
  - admin/src/components/forms/SeriesForm.tsx
  - admin/src/components/forms/EpisodeForm.tsx
  - admin/src/api/seasons.ts
autonomous: true

must_haves:
  truths:
    - "Admin can create a series with basic info (title, description, categories, images)"
    - "Admin can navigate to a series detail page to manage seasons"
    - "Admin can add, edit, and delete seasons for a series"
    - "Admin can navigate to a season page to manage episodes"
    - "Admin can add, edit, and delete episodes with metadata and video reference"
    - "Series list page shows season count for each series"
  artifacts:
    - path: "admin/src/routes/_authenticated/series/index.tsx"
      provides: "Series list page"
      min_lines: 30
    - path: "admin/src/routes/_authenticated/series/$seriesId.tsx"
      provides: "Series detail with season management"
      min_lines: 50
    - path: "admin/src/routes/_authenticated/series/$seriesId.seasons.$seasonId.tsx"
      provides: "Season detail with episode management"
      min_lines: 50
    - path: "admin/src/api/seasons.ts"
      provides: "Season and Episode API client"
      exports: ["listSeasons", "createSeason", "updateSeason", "deleteSeason", "listEpisodes", "createEpisode", "updateEpisode", "deleteEpisode"]
  key_links:
    - from: "admin/src/routes/_authenticated/series/$seriesId.tsx"
      to: "admin/src/api/seasons.ts"
      via: "season CRUD operations"
      pattern: "listSeasons|createSeason"
    - from: "admin/src/routes/_authenticated/series/$seriesId.seasons.$seasonId.tsx"
      to: "admin/src/api/seasons.ts"
      via: "episode CRUD operations"
      pattern: "listEpisodes|createEpisode"
---

<objective>
Build the Series management pages with hierarchical navigation: Series list -> Series detail (seasons) -> Season detail (episodes).

Purpose: Series are the most complex content type with a three-level hierarchy. This plan creates the complete admin UI for managing series, seasons, and episodes as separate navigable pages per the user's decision.
Output: Series list page, series detail page with season management, season detail page with episode management. Full CRUD at each level.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-api-and-admin-content-management/03-RESEARCH.md
@.planning/phases/03-content-api-and-admin-content-management/03-05-SUMMARY.md
@.planning/phases/03-content-api-and-admin-content-management/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Season/Episode API client and Series form</name>
  <files>
    admin/src/api/seasons.ts
    admin/src/components/forms/SeriesForm.tsx
    admin/src/components/forms/EpisodeForm.tsx
  </files>
  <action>
    1. Create admin/src/api/seasons.ts:
       - `listSeasons(contentId)`: GET /api/admin/content/:contentId/seasons
       - `createSeason(contentId, data)`: POST /api/admin/content/:contentId/seasons
       - `updateSeason(contentId, seasonId, data)`: PUT /api/admin/content/:contentId/seasons/:seasonId
       - `deleteSeason(contentId, seasonId)`: DELETE /api/admin/content/:contentId/seasons/:seasonId
       - `listEpisodes(contentId, seasonId)`: GET /api/admin/content/:contentId/seasons/:seasonId/episodes
       - `createEpisode(contentId, seasonId, data)`: POST /api/admin/content/:contentId/seasons/:seasonId/episodes
       - `updateEpisode(contentId, seasonId, episodeId, data)`: PUT /api/admin/content/:contentId/seasons/:seasonId/episodes/:episodeId
       - `deleteEpisode(contentId, seasonId, episodeId)`: DELETE /api/admin/content/:contentId/seasons/:seasonId/episodes/:episodeId
       - Define Season and Episode TypeScript types

    2. Create admin/src/components/forms/SeriesForm.tsx:
       - Reuses structure from MovieForm but for SERIES type
       - Fields: title, description, releaseYear, ageRating, quality, categories, cast, director, trailerUrl, poster image, backdrop image, isPublished
       - No duration field (duration is per-episode)
       - No videoUrl field (videos are per-episode)
       - React Hook Form + Zod
       - Props: mode, defaultValues, onSuccess

    3. Create admin/src/components/forms/EpisodeForm.tsx:
       - Small inline form or dialog for creating/editing an episode
       - Fields: number (required), title (required), description (optional), duration (optional, minutes), videoUrl (optional), thumbnail (optional, could be a simple URL input for now)
       - React Hook Form + Zod
       - Props: mode, defaultValues, onSubmit, onCancel
       - Rendered as a shadcn Dialog/Sheet so it overlays the episode list
  </action>
  <verify>
    `cd admin && npx tsc --noEmit` compiles clean.
  </verify>
  <done>Season/Episode API client functions created. SeriesForm component ready for create/edit pages. EpisodeForm dialog component ready for inline episode management.</done>
</task>

<task type="auto">
  <name>Task 2: Series list, series detail, and season detail pages</name>
  <files>
    admin/src/routes/_authenticated/series/index.tsx
    admin/src/routes/_authenticated/series/new.tsx
    admin/src/routes/_authenticated/series/$seriesId.tsx
    admin/src/routes/_authenticated/series/$seriesId.seasons.$seasonId.tsx
  </files>
  <action>
    1. admin/src/routes/_authenticated/series/index.tsx:
       - Same pattern as Movies list page but type='SERIES'
       - ContentCard shows season count badge (from _count.seasons in API response)
       - "Add Series" button navigates to /series/new
       - Click on a series navigates to /series/{id} (NOT edit form -- goes to series detail for season management)
       - Edit action in dropdown/hover still goes to /series/{id} (which is the detail+edit page)

    2. admin/src/routes/_authenticated/series/new.tsx:
       - Renders SeriesForm in create mode
       - Page title: "Add Series"
       - onSuccess: navigate to /series/{newId} so admin can immediately add seasons

    3. admin/src/routes/_authenticated/series/$seriesId.tsx:
       - Combined series detail + edit page
       - Top section: SeriesForm in edit mode with current series data (collapsible or always visible)
       - Below form: "Seasons" section with:
         - List of seasons as cards/rows showing: Season {number}, title (if any), episode count
         - "Add Season" button: opens inline form or dialog for season number + title, calls createSeason
         - Each season has: Edit (inline), Delete (with confirm), and "Manage Episodes" link -> navigates to /series/{id}/seasons/{seasonId}
         - Seasons sorted by number
       - Breadcrumb: Series > {Series Title}
       - Back link to /series

    4. admin/src/routes/_authenticated/series/$seriesId.seasons.$seasonId.tsx:
       - Season detail page showing episodes
       - Header: "Season {number}" with edit (inline number/title edit) and delete button
       - Episode list as a table or card list:
         - Columns: Episode number, title, duration, video status (has URL or not)
         - "Add Episode" button: opens EpisodeForm dialog
         - Each episode: Edit (opens EpisodeForm dialog with defaults), Delete (with confirm)
         - Episodes sorted by number
       - Breadcrumb: Series > {Series Title} > Season {number}
       - Back link to /series/{seriesId}

    Navigation flow: /series -> /series/new (create) or /series/{id} (detail) -> /series/{id}/seasons/{seasonId} (episodes)

    Use TanStack Query for all data fetching with appropriate query keys for cache invalidation.
    Show loading skeletons. Show toast on all CRUD operations.
  </action>
  <verify>
    `cd admin && npx tsc --noEmit` compiles clean.
    Start both servers.
    Navigate to /series -- see series list (or empty state).
    Create a new series via /series/new -- redirected to series detail.
    Add a season from the detail page -- appears in the season list.
    Navigate to season detail -- see episode list (empty).
    Add an episode -- appears in the list.
    Edit and delete operations work with confirmations and toasts.
    Breadcrumb navigation works at all levels.
  </verify>
  <done>Complete series management hierarchy: list -> detail (with season management) -> season detail (with episode management). All CRUD operations functional with proper navigation, breadcrumbs, and feedback.</done>
</task>

</tasks>

<verification>
- TypeScript compiles clean
- Series list page shows series with season counts
- Create series form works, redirects to detail page
- Series detail shows editable series info + season list
- Add/edit/delete seasons works
- Season detail page shows episode list
- Add/edit/delete episodes works via dialog
- Breadcrumb navigation works at all three levels
- All changes persist via API (refresh shows same data)
</verification>

<success_criteria>
Complete three-level series management (series -> seasons -> episodes) with full CRUD at each level, proper navigation hierarchy, and API integration.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-api-and-admin-content-management/03-08-SUMMARY.md`
</output>
