---
phase: 03-content-api-and-admin-content-management
plan: 09
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - admin/src/routes/_authenticated/settings/categories.tsx
autonomous: false

must_haves:
  truths:
    - "Admin can view all categories in a list"
    - "Admin can create a new category"
    - "Admin can edit a category name"
    - "Admin can delete a category with confirmation"
    - "Duplicate category names show an error"
  artifacts:
    - path: "admin/src/routes/_authenticated/settings/categories.tsx"
      provides: "Category management settings page"
      min_lines: 40
  key_links:
    - from: "admin/src/routes/_authenticated/settings/categories.tsx"
      to: "admin/src/api/categories.ts"
      via: "CRUD operations"
      pattern: "listCategories|createCategory|updateCategory|deleteCategory"
---

<objective>
Build the Categories settings page where admins can manage content categories (create, edit, delete).

Purpose: Categories are used in content forms as dropdown options. Admins need a settings page to manage the list of available categories.
Output: Settings/Categories page with category CRUD, inline editing, and delete confirmation.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-api-and-admin-content-management/03-02-SUMMARY.md
@.planning/phases/03-content-api-and-admin-content-management/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Categories settings page</name>
  <files>
    admin/src/routes/_authenticated/settings/categories.tsx
  </files>
  <action>
    Create the categories management page at /settings/categories:

    1. Page layout:
       - Page title: "Categories" with subtitle "Manage content categories and genres"
       - "Add Category" button at the top

    2. Category list:
       - Simple list/table of categories showing: name, slug (auto-generated, shown as gray text), created date
       - Each row has Edit and Delete action buttons (icon buttons)
       - Sorted alphabetically by name

    3. Add category:
       - Click "Add Category" shows an inline form at the top of the list (or a small dialog)
       - Single text input for category name
       - Submit calls createCategory, invalidates query, shows success toast
       - If duplicate name (409 from API), show error toast "Category already exists"

    4. Edit category:
       - Click Edit button makes the name cell editable (inline edit)
       - Press Enter or click Save to update, Escape to cancel
       - Calls updateCategory, invalidates query, shows success toast
       - Handle duplicate name error

    5. Delete category:
       - Click Delete shows AlertDialog: "Delete category '{name}'? This won't remove the category from existing content."
       - Confirm calls deleteCategory, invalidates query, shows success toast

    Use TanStack Query with key ['categories'] for data fetching.
    Show loading skeleton while loading.
    Empty state: "No categories yet. Create your first category to organize content."

    This is a simple CRUD page -- keep it clean and functional. No need for pagination (categories are a small dataset).
  </action>
  <verify>
    `cd admin && npx tsc --noEmit` compiles clean.
    Start both servers. Navigate to /settings/categories.
    Add a category "Action" -- appears in the list.
    Add a category "Action" again -- error toast "Category already exists".
    Edit "Action" to "Action & Adventure" -- updates in list.
    Delete "Action & Adventure" -- removed from list after confirmation.
  </verify>
  <done>Categories settings page with full CRUD, inline editing, delete confirmation, and duplicate handling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete admin panel with login, sidebar navigation, content management for all 4 types (Movies, Series, Documentaries, TV Channels), image upload, categories settings, grid/table views, filtering, and sorting.</what-built>
  <how-to-verify>
    Prerequisites: Both API (port 5000) and admin panel (port 3001) running. Database seeded with admin user.

    1. Visit http://localhost:3001 -- should redirect to login page
    2. Log in with admin@lumio.tv / AdminPass123! -- should see dashboard with sidebar
    3. Navigate to Settings > Categories:
       - Add categories: "Action", "Drama", "Comedy", "Thriller", "Documentary"
    4. Navigate to Movies:
       - Click "Add Movie", fill in all fields, upload a poster image (drag and drop any JPG/PNG), save
       - Verify movie appears in the grid with poster thumbnail
       - Toggle to table view -- verify columns and sorting work
       - Filter by status (Draft/Published)
       - Search by title
    5. Navigate to Series:
       - Create a series, then add a season, then add episodes to the season
       - Verify breadcrumb navigation works
    6. Navigate to Documentaries and TV Channels:
       - Create one of each, verify the forms and listing work
    7. Check that poster images display correctly in the card grid
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Categories page CRUD works
- Categories appear in content form dropdowns
- All content types can be created, edited, deleted from admin panel
- End-to-end flow verified by human
</verification>

<success_criteria>
Categories management page fully functional. Phase 3 complete with full admin content management capability verified by human testing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-api-and-admin-content-management/03-09-SUMMARY.md`
</output>
