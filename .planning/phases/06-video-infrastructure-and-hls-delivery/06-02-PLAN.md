---
phase: 06-video-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - api/src/services/r2.service.ts
  - api/src/routes/video-upload.routes.ts
  - api/src/routes/index.ts
autonomous: true

must_haves:
  truths:
    - "API can generate presigned PUT URLs for direct browser-to-R2 upload"
    - "API can generate presigned GET URLs for R2 objects"
    - "Upload confirmation downloads file from R2 and runs ffprobe to validate video codec/format"
    - "Invalid files are deleted from R2 and rejected with an error"
    - "Admin upload stores sourceVideoKey on Content/Episode record"
  artifacts:
    - path: "api/src/services/r2.service.ts"
      provides: "R2 operations: presign upload, presign download, upload buffer, delete objects, list objects, stream to file"
      exports: ["generatePresignedUploadUrl", "generatePresignedDownloadUrl", "uploadToR2", "deleteFromR2", "deleteR2Prefix", "listR2Objects", "streamR2ToFile"]
    - path: "api/src/routes/video-upload.routes.ts"
      provides: "POST /presign (generate upload URL), POST /confirm (ffprobe validate + store key)"
      exports: ["videoUploadRouter"]
  key_links:
    - from: "api/src/services/r2.service.ts"
      to: "api/src/config/r2.ts"
      via: "import r2Client"
      pattern: "import.*r2Client.*from.*config/r2"
    - from: "api/src/routes/video-upload.routes.ts"
      to: "api/src/services/r2.service.ts"
      via: "generatePresignedUploadUrl, streamR2ToFile, deleteFromR2"
      pattern: "generatePresignedUploadUrl|streamR2ToFile|deleteFromR2"
---

<objective>
Create the R2 storage service and video upload API endpoints so admins can upload video files directly to R2 via presigned URLs.

Purpose: Direct browser-to-R2 upload avoids streaming large video files (up to 5GB) through the Express server. The R2 service is also used by transcoding (Plan 03) and streaming (Plan 04).
Output: R2 service with all CRUD operations, two video upload endpoints (presign + confirm with ffprobe validation).
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-video-infrastructure-and-hls-delivery/06-RESEARCH.md
@api/src/config/r2.ts
@api/src/config/env.ts
@api/src/routes/index.ts
@api/src/middleware/auth.middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: R2 storage service</name>
  <files>api/src/services/r2.service.ts</files>
  <action>
Create api/src/services/r2.service.ts with these functions:

1. generatePresignedUploadUrl(key: string, contentType: string, expiresIn = 3600): Promise<string>
   - Use @aws-sdk/s3-request-presigner getSignedUrl with PutObjectCommand
   - Set ContentType on the command
   - Returns the presigned URL string

2. generatePresignedDownloadUrl(key: string, expiresIn = 14400): Promise<string>
   - 14400 = 4 hours default TTL (per context decisions)
   - Use GetObjectCommand with getSignedUrl

3. uploadToR2(key: string, body: Buffer | Readable, contentType: string, cacheControl?: string): Promise<void>
   - Use PutObjectCommand directly (for uploading transcoded segments from server)
   - Accept optional cacheControl parameter and set CacheControl on the PutObjectCommand if provided

4. deleteFromR2(key: string): Promise<void>
   - Use DeleteObjectCommand

5. deleteR2Prefix(prefix: string): Promise<void>
   - Use ListObjectsV2Command to list all objects with prefix
   - Delete each with DeleteObjectCommand
   - Handle pagination (IsTruncated + ContinuationToken)

6. listR2Objects(prefix: string): Promise<string[]>
   - Use ListObjectsV2Command, return array of keys
   - Handle pagination

7. headR2Object(key: string): Promise<{ contentLength: number; contentType: string } | null>
   - Use HeadObjectCommand, return metadata or null if not found
   - Catch NoSuchKey error and return null

8. streamR2ToFile(key: string, destPath: string): Promise<void>
   - Use GetObjectCommand to get the object
   - Stream the Body (Readable) to destPath using pipeline from "node:stream/promises" and createWriteStream from "node:fs"
   - This is used by the confirm endpoint (ffprobe validation) and the transcode job (downloading source)

All functions import r2Client and R2_BUCKET_NAME from ../config/r2.js.
Import commands from @aws-sdk/client-s3 and getSignedUrl from @aws-sdk/s3-request-presigner.
Use ESM imports with .js extensions.
  </action>
  <verify>npx tsc --noEmit (from api directory)</verify>
  <done>R2 service exports all storage operations needed by upload, transcode, and stream features</done>
</task>

<task type="auto">
  <name>Task 2: Video upload presign and confirm endpoints</name>
  <files>api/src/routes/video-upload.routes.ts, api/src/routes/index.ts</files>
  <action>
Create api/src/routes/video-upload.routes.ts:

1. POST /api/admin/video-upload/presign
   - Protected by requireAuth + requireAdmin
   - Body: { contentId: string, episodeId?: string, filename: string, contentType: string, fileSize: number }
   - Validate contentType is video/mp4, video/x-matroska, or video/quicktime
   - Validate fileSize <= 5 * 1024 * 1024 * 1024 (5GB)
   - Verify content exists (and episode exists if episodeId provided)
   - Generate R2 key: key = `videos/${contentId}${episodeId ? '/episodes/' + episodeId : ''}/raw/source.${ext}`
     where ext = filename.split('.').pop()
   - Call generatePresignedUploadUrl(key, contentType)
   - Return { uploadUrl: string, key: string }

2. POST /api/admin/video-upload/confirm
   - Protected by requireAuth + requireAdmin
   - Body: { contentId: string, episodeId?: string, key: string }
   - Verify the file exists in R2 via headR2Object(key)
   - If not found, return 400 "File not found in storage"
   - Run ffprobe validation (LOCKED DECISION -- server-side ffprobe check is required):
     a. Create temp file path: /tmp/ffprobe-validate-${Date.now()}${path.extname(key)}
     b. Download the file from R2 to temp path using streamR2ToFile(key, tempPath)
        - For very large files this is slow, but validation correctness is required
     c. Spawn ffprobe: ["ffprobe", "-v", "quiet", "-print_format", "json", "-show_streams", "-show_format", tempPath]
     d. Parse JSON output, find video stream (codec_type === "video")
     e. If no video stream found, or codec_name is not in allowed list ["h264", "hevc", "vp9", "mpeg4", "prores", "dnxhd", "vp8", "av1"]:
        - Delete the file from R2: deleteFromR2(key)
        - Delete temp file
        - Return 400 "Invalid video file: no valid video stream detected"
     f. If valid, delete temp file
   - Update database:
     - If episodeId: update Episode set sourceVideoKey = key
     - Else: update Content set sourceVideoKey = key
   - Return { success: true, key: string }

   IMPORTANT: Wrap the ffprobe/download logic in try/finally to always clean up the temp file.
   Import spawn from "node:child_process", path from "node:path", { unlink } from "node:fs/promises".
   Import { streamR2ToFile, deleteFromR2 } from "../services/r2.service.js".

Register in api/src/routes/index.ts:
- Import videoUploadRouter
- app.use("/api/admin/video-upload", videoUploadRouter)

Use Zod schemas for request validation inline in the route file.
Use Express 5 Router (import { Router } from "express").
Apply requireAuth and requireAdmin middleware via router.use().
  </action>
  <verify>
   - npx tsc --noEmit
   - API starts without errors: docker compose restart api && docker compose logs api --tail 20
   - curl POST /api/admin/video-upload/presign with valid admin JWT returns 200 with uploadUrl (will fail with R2 placeholder creds but route should be reachable -- check for 500 not 404)
  </verify>
  <done>Two video upload endpoints registered, confirm endpoint downloads file from R2, runs ffprobe validation, rejects invalid files (deleting from R2), and stores sourceVideoKey on valid files</done>
</task>

</tasks>

<verification>
- R2 service compiles and exports all 8 functions (including streamR2ToFile)
- Video upload routes registered at /api/admin/video-upload/*
- Routes protected by admin auth middleware
- Confirm endpoint runs ffprobe on downloaded R2 file
- Invalid files are deleted from R2 and rejected
- TypeScript compiles clean
- API starts without errors
</verification>

<success_criteria>
- generatePresignedUploadUrl and generatePresignedDownloadUrl work with R2 endpoint format
- Presign endpoint validates file type (MP4/MKV/MOV) and size (5GB max)
- Confirm endpoint downloads file from R2, runs ffprobe, validates video stream exists
- Invalid files deleted from R2 with 400 error response
- sourceVideoKey is stored on Content or Episode record
- Temp files always cleaned up (try/finally)
- All imports use .js extensions (ESM)
</success_criteria>

<output>
After completion, create `.planning/phases/06-video-infrastructure-and-hls-delivery/06-02-SUMMARY.md`
</output>
