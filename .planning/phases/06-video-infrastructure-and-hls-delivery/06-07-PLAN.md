---
phase: 06-video-infrastructure
plan: 07
type: execute
wave: 5
depends_on: ["06-04", "06-05", "06-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "API builds and starts with all new routes and worker"
    - "Admin panel builds with VideoUploader and TranscodingBadge"
    - "Client builds with stream endpoint integration"
    - "All TypeScript compiles clean across all workspaces"
  artifacts: []
  key_links: []
---

<objective>
Build verification and end-to-end smoke test of the entire video infrastructure pipeline.

Purpose: Verify all three workspaces compile, API starts with worker, and the pipeline is ready for real R2 credentials to test with actual video files.
Output: Confirmed builds, verified API routes, human verification of admin UI.
</objective>

<execution_context>
@C:\Users\user\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\user\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification across all workspaces</name>
  <files></files>
  <action>
1. Verify API compiles:
   - cd api && npx tsc --noEmit
   - Fix any TypeScript errors

2. Verify admin panel builds:
   - cd admin && npx vite build
   - Fix any build errors

3. Verify client builds:
   - cd client && npx next build
   - Fix any build errors

4. Verify Docker containers start:
   - docker compose up -d
   - docker compose ps (all healthy)
   - docker compose logs api --tail 50 (no errors, worker started)

5. Verify API routes exist:
   - curl http://localhost:5000/health (200)
   - Login as admin, get JWT
   - curl -H "Authorization: Bearer $TOKEN" http://localhost:5000/api/admin/video-upload/presign -X POST -H "Content-Type: application/json" -d '{"contentId":"test","filename":"test.mp4","contentType":"video/mp4","fileSize":1000}' (should return 404 for content not found, proving the route is registered and validation runs)
   - curl -H "Authorization: Bearer $TOKEN" http://localhost:5000/api/stream/nonexistent (should return 404, proving route is registered)

6. Verify schema migration:
   - npx prisma migrate status (all applied)
   - Check that Content table has transcodingStatus column
  </action>
  <verify>
   - All three workspaces compile/build without errors
   - Docker containers healthy
   - API routes reachable
   - Schema migration applied
  </verify>
  <done>All workspaces build clean, API starts with transcode worker, routes registered, schema migrated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete video infrastructure pipeline: R2 storage, FFmpeg transcoding, BullMQ job queue, presigned URL streaming, admin video upload UI, client player integration</what-built>
  <how-to-verify>
    1. Open admin panel at http://localhost:3001
    2. Navigate to edit a movie — verify VideoUploader component appears below image upload fields
    3. Verify TranscodingBadge area is visible (will show nothing for existing content without transcoding)
    4. Navigate to a series -> season -> edit an episode — verify VideoUploader appears in the episode dialog
    5. Note: Actual upload testing requires real R2 credentials. The UI and API plumbing is in place.
    6. Check API logs (docker compose logs api) — verify "Transcode worker started" appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All TypeScript clean across api, admin, client
- Docker: postgres, redis, api all healthy
- API logs show worker started, no Redis errors
- Video upload routes return expected errors (not 404)
- Stream route returns expected error (not 404)
- Admin UI shows VideoUploader on movie/episode forms
</verification>

<success_criteria>
- Zero TypeScript errors across all workspaces
- API starts and serves all new endpoints
- Transcode worker running
- Admin panel shows video upload UI
- Pipeline ready for real R2 credentials and test video
</success_criteria>

<output>
After completion, create `.planning/phases/06-video-infrastructure-and-hls-delivery/06-07-SUMMARY.md`
</output>
